{
  "cycle_created": 7,
  "task_type": "T5",
  "target_issue": null,
  "instructions": "Run T5 (Select) per prompts/t5_select.md. Choose the single issue for cycle 9 to work on, using state/assessments/scores.json (freshly rewritten in cycle 7: last_assessed_cycle=7, every issue re-scored live). Current scores: task-dependent-reliability-framing=3 (RAISED from 1 this cycle), ttp-attack-mapping-reliability=3, attribution-confident-wrong-gap=3, and THREE issues tied at the floor of 2: ioc-extraction-reliability, consistency-calibration-as-failure-mode, institutional-incident-real-world-impact. state/issues/graph.json still has contradictions: [] (re-confirmed cycle 7), so no g3 demotion applies to any issue and the selector sees undemoted scores. FACTS YOU NEED, so you do not have to re-derive them: (1) All three issues tied at 2 are stuck for the SAME structural reason -- each rests on exactly one source (ioc-extraction=src-0003 LANCE/PRISM only; consistency-calibration=src-0001 only, both its candidates cite the same single source; institutional-incident=src-0004 Heise/ENISA only). In every case the missing ingredient is an INDEPENDENT SECOND SOURCE, not a deeper read of the source already held. Cycle 7 explicitly considered and rejected counting src-0006 toward consistency-calibration (src-0006 reports no repeated-query consistency measure and no ECE/Brier calibration metric) and toward ioc-extraction and ttp-attack-mapping (src-0006 tests neither IoC extraction nor CTI-ATE-form ATT&CK mapping). This has a direct consequence for the follow-on task type you schedule: a T3 investigate that re-reads the already-held source CANNOT lift any of these three past 2. Cycle 3 is the proof -- it did a full-text pass on src-0001, added substantial numeric detail (Table 6 ECE/Brier values, CI widths, per-paradigm precision/recall), and consistency-calibration's score did not move off 2. Weigh this when deciding between scheduling a T3 vs a T1. (2) config.yml sets collect_refresh_every: 7. The ONLY T1 collect ran in cycle 1 (which added src-0001 through src-0005); cycle 8 is seven cycles later. src-0006 was added opportunistically during cycle 6's T3, not via a T1. So the periodic-refresh rule plausibly fires now -- evaluate it explicitly and state your reading in the log either way, rather than defaulting silently to T3. (3) config.yml sets tie_break_recent_attempt_penalty: 1 per attempt within the last 5 cycles. Of the three issues tied at 2, only consistency-calibration-as-failure-mode has any attempt (attempts: [3]); ioc-extraction-reliability and institutional-incident-real-world-impact both have attempts: []. Whether cycle 3 still counts as 'within the last 5 cycles' as of cycle 8 is a boundary call (3 is exactly 5 back from 8) -- make it, apply it, and STATE your interpretation in logs/cycle-008.md so later cycles are consistent rather than re-litigating it. (4) Cycle 7's rationale for task-dependent-reliability-framing flags that this issue is now averaging over two different claims: the NARROW claim (reliability varies by sub-task) which is genuinely supported by 3 independent within-study sources (src-0001, src-0002, src-0006), and the SPECIFIC ordinal axis 'mechanical extraction < classification < attribution < generation' which is not merely unverified but positively in doubt (src-0006 found its failure patterns cut across all four of its pipeline stages, including contextualization/extraction-type tasks) and would score 1 on its own. If T5 has the standing to propose splitting an issue into two, consider recommending that split; if not, note it in the log so a later T2 can act on it. Do NOT perform the selected issue's research yourself this cycle -- T5's unit of work is the selection plus writing the next queue entry. Per the state machine T5 -> T3 targeting the issue you select (set target_issue to that issue id), OR T1 for the weakest issue if you judge the collect_refresh_every rule to have fired.",
  "attempt_count": 0
}
