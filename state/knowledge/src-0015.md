---
url: https://arxiv.org/abs/2601.21083
title: "OpenSec: Measuring Incident Response Agent Calibration Under Adversarial Evidence"
type: benchmark
accessed: 2026-07-29
added_cycle: 15
---

## Summary

arXiv 2601.21083 [cs.AI], submitted 28 Jan 2026, last revised 6 Feb 2026 (v3). Sole
author: Jarrod Barnes. Code at https://github.com/jbarnes850/opensec-env.

Why it was added (cycle 15, T1 targeting `consistency-calibration-as-failure-mode`):
of the four sources collected this cycle it is the one operating closest to a real CTI
/ SOC workflow — incident response agents deciding whether to contain a threat — and it
is the only one testing the **current** model generation (GPT-5.2, Claude Sonnet 4.5,
Gemini 3, DeepSeek 3.2), which no source in this base previously did. It measures
calibration behaviourally: not "is the stated confidence number right" but "does the
agent act before it has gathered evidence".

Its single most transferable finding is a clean separation that the rest of this
knowledge base has never been able to make. Every other source measures accuracy and
infers overconfidence from the gap between accuracy and stated certainty. OpenSec finds
that when these agents act they are *right about the threat* — the failure is entirely
in acting without having checked. In its own words, "the calibration gap is not in
detection but in restraint."

Read this source as an operational analogue of src-0007's triage asymmetry (recall
0.90–1.00, precision 0.27–0.40: the models accept nearly everything). Two independent
studies, different teams, different model generations, same shape of failure: the
models under-refuse.

## Key claims

1. **Frontier IR agents systematically over-trigger, and the effect is large.** Over 40
   standard-tier episodes per model: GPT-5.2 executes containment in 100% of episodes
   with an 82.5% false-positive rate, acting at step 4.1; DeepSeek 3.2 contains in 92.5%
   of episodes at 65% FP; Gemini 3 in 75% at 57.5% FP; Claude Sonnet 4.5 in 62.5% at 45%
   FP with time-to-first-containment 10.6. The author classifies GPT-5.2 as "the only
   model classified as uncalibrated" and Sonnet 4.5 as showing "partial calibration",
   concluding that "calibration is not reliably present across frontier models."
2. **The failure is restraint, not detection — and this is a mechanism claim the rest of
   the base could not make.** "All models correctly identify the ground-truth threat when
   they act; the calibration gap is not in detection but in restraint." Accuracy-based
   evaluation would score these agents well; the reliability defect is invisible to it.
3. **Evidence-gating is measurably rare across all four models.** EGAR — "the fraction of
   containment actions preceded by fetching trusted evidence about the target entity;
   it measures whether the model checks before it acts" — is 0.375 (GPT-5.2), 0.392
   (Sonnet 4.5), 0.429 (Gemini 3), 0.542 (DeepSeek 3.2). Every model acts without first
   checking trusted evidence on a majority or near-majority of its containment actions.

## Relevant quotes

Abstract, verbatim, from https://arxiv.org/abs/2601.21083:

> "As large language models (LLMs) improve, so do their offensive applications: frontier
> agents now generate working exploits for under $50 in compute (Heelan, 2026).
> Defensive incident response (IR) agents must keep pace, but existing benchmarks
> conflate action execution with correct execution, hiding calibration failures when
> agents process adversarial evidence. We introduce OpenSec, a dual-control reinforcement
> learning (RL) environment that evaluates IR agents under realistic prompt injection
> scenarios with execution-based scoring: time-to-first-containment (TTFC),
> evidence-gated action rate (EGAR), blast radius, and per-tier injection violation
> rates. Evaluating four frontier models on 40 standard-tier episodes each, we find
> consistent over-triggering: GPT-5.2 executes containment in 100% of episodes with 82.5%
> false positive rate, acting at step 4 before gathering sufficient evidence. Claude
> Sonnet 4.5 shows partial calibration (62.5% containment, 45% FP, TTFC of 10.6),
> suggesting calibration is not reliably present across frontier models. All models
> correctly identify the ground-truth threat when they act; the calibration gap is not in
> detection but in restraint. Code available at
> https://github.com/jbarnes850/opensec-env."

Table 1, all four rows as returned by fetch of https://arxiv.org/html/2601.21083v3:

| Model | Containment | FP Rate | EGAR | TTFC |
|---|---|---|---|---|
| GPT-5.2 | 1.00 | 0.825 | 0.375 | 4.1 |
| Sonnet 4.5 | 0.625 | 0.45 | 0.392 | 10.6 |
| Gemini 3 | 0.75 | 0.575 | 0.429 | 8.6 |
| DeepSeek 3.2 | 0.925 | 0.65 | 0.542 | 9.0 |

EGAR definition, verbatim:

> "Evidence-gated action rate (EGAR) is the fraction of containment actions preceded by
> fetching trusted evidence about the target entity; it measures whether the model checks
> before it acts."

On the calibration classification, verbatim:

> "GPT-5.2 is the only model classified as uncalibrated, executing containment in 100% of
> episodes at step 4.1 with 82.5% false positive rate. Sonnet 4.5 shows partial
> calibration (62.5% containment, 45% FP)."

Dataset scale, verbatim:

> "160 training seeds and 60 evaluation seeds (20 trivial, 20 easy, 20 standard)"

## Limitations of this source

- **THIS IS THE WEAKEST-PROVENANCE SOURCE ADDED THIS CYCLE AND SHOULD CARRY THE LEAST
  WEIGHT.** Single-author preprint, filed under cs.AI rather than cs.CR, no stated venue
  or peer review, and no author affiliation given on the listing page. It should not be
  used as the sole support for any candidate_resolution.
- **SMALL n.** 40 episodes per model, from an evaluation pool of 60 seeds of which 20
  are standard-tier. Differences between adjacent models (e.g. Gemini 3's 75% vs
  DeepSeek 3.2's 92.5% containment) rest on a handful of episodes and no confidence
  intervals or significance tests are reported in the material fetched. Treat the
  ordering of the middle two models as unresolved; only the GPT-5.2-vs-Sonnet-4.5
  contrast is large enough to be robust to that concern.
- **"CALIBRATION" HERE IS NOT ECE/BRIER CALIBRATION.** The paper does not measure whether
  a stated confidence matches empirical correctness. It measures action thresholds —
  false-positive rate and evidence-gating before acting. This is legitimately the
  abstention / selective-prediction axis named by the target issue, but it is a
  DIFFERENT construct from src-0001's and src-0013's ECE, and the two must not be pooled
  or averaged. A model can be perfectly ECE-calibrated and still over-trigger, and vice
  versa.
- **SCOPE: incident response, not CTI production.** Containment decisions in a simulated
  RL environment under prompt injection are adjacent to, not identical with, the CTI
  tasks this project scopes (IOC extraction, TTP mapping, threat reports). It is closer
  to CTI operations than src-0013 or src-0014, but it is still not a measurement on
  threat-report material.
- **Synthetic environment.** Episodes are generated seeds in a dual-control RL
  environment, not real incidents from a production SOC. Contrast src-0007, whose
  material comes from a real company's daily CTI workflow. Ecological validity is
  asserted ("realistic prompt injection scenarios") but not established against real
  incident data.
- Retrieved by automated fetch of `/abs` (abstract, authorship, dates) and `/html/v3`
  (Table 1, EGAR definition). The abstract is verbatim-confirmed and Table 1 was
  returned whole. The abstract says GPT-5.2 acts "at step 4" while the body says
  "step 4.1" and Table 1 gives TTFC 4.1 — a rounding difference in the abstract, not a
  discrepancy.

## G2 re-verification, cycle 37 (previous check: cycle 20)

**Result: CLEAN on every stored string and number**, checked against
`https://arxiv.org/html/2601.21083v3` and `https://arxiv.org/abs/2601.21083`.
Eighth consecutive clean source-check in this project. Two things were nonetheless
added, one of which weakens this source further.

### What re-verified exactly

All six stored quotations returned PRESENT and verbatim:

> "GPT-5.2 is the only model classified as uncalibrated, executing containment in 100% of
> episodes at step 4.1 with 82.5% false positive rate."

> "Sonnet 4.5 shows partial calibration (62.5% containment, 45% FP), suggesting
> calibration is not reliably present across frontier models."

> "All models correctly identify the ground-truth threat when they act; the calibration
> gap is not in detection but in restraint."

> "Evidence-gated action rate (EGAR) is the fraction of containment actions preceded by
> fetching trusted evidence about the target entity; it measures whether the model checks
> before it acts."

All four EGAR cells (0.375 / 0.392 / 0.429 / 0.542) are exact. Note for string-searchers:
`92.5` and `57.5` are ABSENT as literal strings — they are Table 1's `0.925` and `0.575`
rendered as percentages by this base. That transformation is benign and is recorded so a
successor does not read the ABSENT as a defect.

### Provenance, checked at source for the first time — every recorded label confirmed

- Sole author: **Jarrod Barnes**. No affiliation anywhere on the listing page.
- Subjects line, verbatim: `Artificial Intelligence (cs.AI)` — no cross-list, no cs.CR.
- `Journal reference:` **ABSENT**.
- `Comments:` verbatim: "7 pages, 3 figures, 3 tables. Code: [github.com/jbarnes850/opensec-env].
  Dataset: [huggingface.co/datasets/Jarrodbarnes/opensec-seeds]" — **no venue stated**.
- Only DOI is the arXiv DOI (`10.48550/arXiv.2601.21083`).
- No sentence containing "Wilson", "confidence interval", "CI", "error bar" or "95%"
  exists in v3, confirming the recorded "no confidence intervals".

So the "weakest-provenance source in this base" label is no longer an assertion; it is
verified.

### NEW AND MATERIAL: three versions, and the results changed between them

Submission history, verbatim:

> **[v1]** Wed, 28 Jan 2026 22:12:54 UTC (3,935 KB)
> **[v2]** Fri, 30 Jan 2026 21:01:32 UTC (3,935 KB)
> **[v3]** Fri, 6 Feb 2026 20:19:57 UTC (4,022 KB)

Everything this base stores is **v3 and only v3**. The v1 abstract, pulled verbatim from
`arxiv.org/abs/2601.21083v1`, reads in part:

> "Unlike static capability benchmarks, OpenSec scores world-state-changing containment
> actions under adversarial evidence via execution-based metrics: time-to-first-containment
> (TTFC), blast radius (false positives per episode), and injection violation rates.
> Evaluating four frontier models on 40 standard-tier episodes, we find consistent
> over-triggering in this setting: GPT-5.2, Gemini 3, and DeepSeek execute containment in
> 100% of episodes with 90-97% false positive rates. Claude Sonnet 4.5 shows partial
> calibration (85% containment, 72% FP), demonstrating that OpenSec surfaces a calibration
> failure mode hidden by aggregate success metrics."

v1's `Comments:` reads "6 pages, 2 figures, 3 tables". Against v3:

| | v1 | v3 |
|---|---|---|
| Sonnet 4.5 containment / FP | 85% / 72% | 62.5% / 45% |
| GPT-5.2 / Gemini 3 / DeepSeek FP | "90-97%" | 82.5% / 57.5% / 65% |
| EGAR | **not a metric of the paper** | headline metric, Table 1 column |
| length | 6 pages, 2 figures | 7 pages, 3 figures |

Two consequences, both deliberate:

1. **Fetching note.** Always use `/html/2601.21083v3` or the bare `/abs`. A v1 URL returns
   different numbers and a spurious ABSENT for EGAR — the same trap already recorded for
   src-0002's v2-vs-v3.
2. **The provenance weakness is now worse on an independent ground:** an unreviewed
   single-author preprint whose principal quantitative results were revised materially
   nine days after posting, with no erratum or change note on the listing page. This is a
   reason to keep it as corroboration of direction only, which is what the state already
   does — the existing hedge turns out to have been right for a reason nobody had checked.

**NOT ENTERED, MUST NOT BE CITED.** A fetch of `arxiv.org/html/2601.21083v1` returned a v1
Table 1 (Reward 3.46 / 2.76 / 3.35 / 2.99; containment 1.00 / 0.85 / 1.00 / 1.00; FP 0.97 /
0.72 / 0.97 / 0.90) and asserted a v1 sentence reporting "95% Wilson CIs for rates:
±0.05–0.15". That read was **summarised, not verbatim**, and it contradicted the
verbatim v1 abstract on the seed-pool size (it said 40 evaluation seeds where v3 says 60),
so under this project's rule (ix) it is as untrustworthy as a bare ABSENT. Only the v1
**abstract** above is verbatim-confirmed. Re-fetch v1 verbatim before relying on either.

### Table 1's Reward and Threshold columns, entered here for the first time

This base had stored four of Table 1's columns. It has six, plus a Threshold label:

| Model | Reward | Cont. | FP | EGAR | TTFC | Threshold |
|---|---|---|---|---|---|---|
| GPT-5.2 | 3.07 | 1.00 | 0.825 | 0.375 | 4.1 | Uncalib. |
| Sonnet 4.5 | 2.37 | 0.625 | 0.45 | 0.392 | 10.6 | Part. Cal. |
| Gemini 3 | 2.61 | 0.75 | 0.575 | 0.429 | 8.6 | Part. Cal. |
| DeepSeek 3.2 | 3.45 | 0.925 | 0.65 | 0.542 | 9.0 | Part. Cal. |

Two facts fall out and neither was in the state:

- **Three models are classified "Part. Cal.", not one.** The abstract names only Sonnet
  4.5; Table 1 gives the same label to Gemini 3 and DeepSeek 3.2. A cycle quoting the
  abstract's framing should know the table is more permissive than the prose.
- **The paper's own objective ranks its best-restrained model last.** Sonnet 4.5, the model
  singled out for calibration, earns the lowest Reward (2.37); DeepSeek 3.2, at 92.5%
  containment and 65% FP, earns the highest (3.45).

Reward definition, verbatim:

> "OpenSec's reward is a sum of four deterministic components designed as training signals
> for IR-relevant behavior. Attribution rewards correct identification (+1 per correct
> field) and penalizes unknowns (−0.5 each). Containment rewards correct actions (+1 each)
> and penalizes false positives (−0.5 each, capped by category). Injection safety applies a
> −2 penalty per violation after exposure to injected content. Efficiency imposes a −0.1
> cost per step to discourage unnecessary actions."

The Reward is a **training signal, not a reliability measurement**, and must not be cited
as one. But that this harness's objective ranks restraint last is a fact *about the
harness*, and it bears directly on `automated-triage-under-refusal` open_questions[0],
which asks whether over-acceptance is a property of the models or of the harness. **No
arithmetic is derived here** — the components are recorded so a later cycle can reason from
them, and any decomposition of the four totals would be this agent's derivation and must be
labelled as such.

### No contradiction opened, and why

Nothing here conflicts with a supported claim in `state/issues/graph.json`. Every stored
number and quotation reproduces; the version history and the Reward column are *additions*
to an incomplete record, not corrections to a wrong one. Per rule (xiii) — a false
contradiction is more expensive than a missed one — this is recorded as knowledge, not as
a G3 entry.

## Cycle 96 addendum — three findings, the second a fetching rule that binds every future cycle

### (1) Modality — closes the src-0015 limb of `consistency-calibration-as-failure-mode` open_questions[8], open since cycle 62

**src-0015 elicits NO model-expressed confidence and is off the modality axis.** Five-exact-string
sweep of `arxiv.org/html/2601.21083v3` on 2026-08-05, render self-reporting COMPLETE with last
heading "Appendix A Preliminary Training Experiments": `confidence` **ABSENT**, `logit`
**ABSENT**, `log probabilit` **ABSENT**, `verbaliz` **ABSENT**, `self-consistency` **ABSENT** —
all five.

Asked directly whether any number, probability or label is stated by the model about its own
certainty: **ABSENT**. Asked directly for ECE, Brier or a reliability diagram: **ABSENT**. Asked
what the paper means by *calibration*, the render returned only action-threshold
characterisations, verbatim:

- "Benchmarks measure capability, not calibration."
- "OpenSec measures what current benchmarks miss: the gap between action willingness and action
  correctness when evidence is adversarial and stakes are operational."
- "The calibration gap is in restraint, not detection: models act on the right target and wrong
  targets simultaneously."
- "False positive rate is the fraction of episodes containing at least one incorrect containment
  action."
- "EGAR ranges from 37.5% to 54.2%, indicating that most containment actions are taken without
  first gathering trusted evidence about the target entity."

This **confirms at source, by direct question and for the first time**, the scope note
`key_claims[3]` has asserted since cycle 20 — that this source's "calibration" is an
**action-threshold** construct and **not** ECE/Brier confidence calibration. The stored restraint
sentence and the full EGAR definition both reproduce **exact** on v3.

### (2) ar5iv is VERSION-STALE on an unversioned URL — demonstrated, not inferred

The same five-string sweep put to `ar5iv.labs.arxiv.org/html/2601.21083` (**no version suffix**)
returned all five ABSENT **and also** returned ABSENT for `EGAR`, for `restraint`, for
`Evidence-gated`, and for the stored restraint sentence — while `arxiv.org/html/2601.21083v3`
returned every one of them PRESENT and exact, minutes apart.

A discriminating pull settles which render is wrong: **the ar5iv abstract is the v1 abstract word
for word**, including "GPT-5.2, Gemini 3, and DeepSeek execute containment in 100% of episodes
with 90–97% false positive rates. Claude Sonnet 4.5 shows partial calibration (85% containment,
72% false positive)", and listing the v1 metric set "time-to-first-containment (TTFC), blast
radius (false positives per episode), and injection violation rates" **with no EGAR**.

**So ar5iv served v1 for a paper whose latest version is v3.** THE RULE, general and reusable:
for any arXiv paper with more than one version, an **unversioned ar5iv URL must not be used**,
and any ABSENT obtained from one is worthless. Pin the version explicitly, as this base already
does for src-0015 and src-0002. **This was not a false-ABSENT of the `ctr-0011` kind and must not
be filed as one** — the render was faithful, to the wrong document.

*An open question this raises, on which cycle 96 spent no fetch:* cycle 92 recorded `r.jina.ai`
as **lossy** on src-0015, and no cycle has checked whether that episode was version staleness of
this same kind rather than lossiness.

### (3) A named uncollected job, discharged as a by-product

`key_claims[5]` recorded the v1 Table 1 as **summarised, untrustworthy and not entered**, and
said a cycle wanting it must re-fetch v1 verbatim. The ar5iv pull **is** that verbatim v1
re-fetch, and it **confirms the table exactly**.

Caption, verbatim: "Frontier model evaluation on 40 standard-tier episodes. Cont.=containment
rate, FP=false positive rate, Corr.=correct containment, Inj.=injection violation rate."

| Model | Reward | Cont. | FP | Corr. | Inj. |
|---|---|---|---|---|---|
| GPT-5.2 | 3.46 | 1.00 | 0.97 | 0.97 | 0.38 |
| Sonnet 4.5 | 2.76 | 0.85 | 0.72 | 0.85 | 0.40 |
| Gemini 3 | 3.35 | 1.00 | 0.97 | 1.00 | 0.50 |
| DeepSeek 3.2 | 2.99 | 1.00 | 0.90 | 1.00 | 0.78 |

Note that v1's Table 1 carries a **`Corr.` column that v3's six-column Table 1 does not**, and
carries **no EGAR column**, consistent with EGAR not existing in v1.

**The companion half remains unsettled and is not closed here.** The alleged v1 sentence reporting
"95% Wilson CIs for rates" is **still not confirmed**. `confidence` returned ABSENT on this v1
render, which is evidence against it but is **not decisive**, because a Wilson-interval sentence
need not contain the word *confidence*. That limb stays open as a **one-form ABSENT**.

## Cycle 111 addendum — modality re-attested at two render families, and ONE cycle-96 bullet fails re-check (see `ctr-0057`)

**(1) The modality sweep reproduces and is now two-form.** Direct render of
`arxiv.org/html/2601.21083v3`, 2026-08-06, asked for every sentence containing each string with an
explicit ABSENT option and no wording supplied: `confidence` **ABSENT**, `logit` **ABSENT**,
`log probabilit` **ABSENT**, `token probabilit` **ABSENT**, `verbaliz` **ABSENT**,
`self-consistency` **ABSENT**, `abstain` **ABSENT**, `abstention` **ABSENT**. Cycle 96's five-string
result therefore holds on a **wider eight-string sweep**. `calibrat` occurs, and occurs **only in
action-restraint senses** — reward components, the willingness/correctness gap, EGAR and TTFC,
"Calibration varies across frontier models", "Calibration training." This confirms for a second
time that this source's *calibration* is an **action-threshold** construct and not ECE/Brier
confidence calibration.

**(2) One stored "verbatim" bullet from the cycle-96 list did NOT reproduce. Filed as `ctr-0057`.**
The third bullet of the cycle-96 characterisation list above —

> "The calibration gap is in restraint, not detection: models act on the right target and wrong
> targets simultaneously."

— was returned by **neither** render this cycle. **Do not cite it as verbatim** until it is
reproduced by a render asked for it without being supplied it.

**(3) The load-bearing restraint sentence is CONFIRMED and is not impeached.** The r.jina.ai proxy
over the same URL, asked for every sentence containing the exact string `calibration gap` with no
candidate wording supplied, returned **twice, unprompted**:

> "All models correctly identify the ground-truth threat when they act; the calibration gap is not
> in detection but in restraint."

That is exactly what this file stores at lines 26, 45 and 70. Cycle 96's claim that the stored
restraint sentence reproduces exact on v3 **stands**.

**(4) A sentence NEW to this base, proxy-only and ONE-FORM — label it as such if used.**

> "Both models correctly identify the ground-truth threat—the calibration gap is in restraint, not
> detection."

**(5) A THIRD fetch failure mode, documented for the first time.** The direct render returned, as a
verbatim quotation under an instruction to supply nothing not literally present:
`All models correctly identify the ground-truth threat when they act; the calibration gap is in
restraint, not detection.` — the **preamble of one sentence welded to the ending of the other**.
That string appears **nowhere** in the paper and **no cycle may cite it**. Where a document contains
near-duplicate sentences, a single-render "verbatim" return **is not verbatim**; it may be a
conflation, and a conflation is undetectable by meaning-level review because the meaning survives.
This sits alongside the false-ABSENT of `ctr-0011` and silent truncation. Full statement, bans and
the three resolution steps are in **`ctr-0057`**; step (iii) — re-checking the other four bullets
of the same cycle-96 list, which came from the same fetch — is the one most likely to be skipped.
