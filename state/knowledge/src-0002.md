---
url: https://arxiv.org/abs/2406.07599
title: "CTIBench: A Benchmark for Evaluating LLMs in Cyber Threat Intelligence"
type: benchmark
accessed: 2026-07-26
added_cycle: 1
---

## Summary

CTIBench (arXiv 2406.07599) is a benchmark suite built specifically for CTI-relevant
tasks, motivated by the authors' observation that general-purpose LLM benchmarks do not
cover practical CTI work. It bundles five sub-tasks and evaluates five LLMs
(ChatGPT-4/gpt-4-turbo, ChatGPT-3.5/gpt-3.5-turbo, Gemini-1.5, LLAMA3-70B, LLAMA3-8B)
against each. Performance varies sharply by task: models do reasonably on
knowledge-recall-style multiple choice (CTI-MCQ) and CVE-to-CWE root cause mapping
(CTI-RCM), but attack-technique extraction (CTI-ATE, mapping malware behavior to MITRE
ATT&CK techniques) is weak across the board, and threat-actor attribution correctness
lags well behind "plausible-sounding" attribution — a gap directly relevant to the risk
of confident-but-wrong CTI output.

## Key claims

1. Task difficulty is highly uneven: best model scores were CTI-MCQ 71.0% (GPT-4-turbo),
   CTI-RCM 72.0% (GPT-4-turbo), but CTI-ATE (MITRE ATT&CK technique extraction) F1 topped
   out at only 0.6388 (GPT-4-turbo), with weaker models scoring as low as 0.1562
   (LLAMA3-8B).
2. On CTI-TAA (threat actor attribution over 50 real reports), "plausible" attribution
   rates are much higher than "correct" attribution rates for every model tested (e.g.
   GPT-4-turbo: 86% plausible vs 52% correct; Gemini-1.5: 74% plausible vs 38% correct).
   This is a concrete, quantified instance of LLMs producing confident/plausible-sounding
   but factually wrong CTI attributions — directly relevant to hallucination risk in
   attribution use cases.
3. Model scale/quality matters but does not close the gap: even the strongest model
   (GPT-4-turbo) does not exceed ~72% on any task, and MITRE ATT&CK technique extraction
   in particular remains a hard, unsolved task for all five evaluated models.

## Relevant quotes / data

- "existing benchmarks provide general evaluations of LLMs, there are no benchmarks that
  address the practical and applied aspects of CTI-specific tasks" (paper's stated
  motivation).
- Reported scores table (from HTML rendering of arXiv 2406.07599v3):
  CTI-MCQ (accuracy): GPT-4-turbo 71.0%, GPT-3.5-turbo 54.1%, Gemini-1.5 65.4%,
  LLAMA3-70B 65.7%, LLAMA3-8B 61.3%.
  CTI-RCM (accuracy): GPT-4-turbo 72.0%, GPT-3.5-turbo 67.2%, Gemini-1.5 66.6%,
  LLAMA3-70B 65.9%, LLAMA3-8B 44.7%.
  CTI-ATE (F1): GPT-4-turbo 0.6388, GPT-3.5-turbo 0.3108, Gemini-1.5 0.4612,
  LLAMA3-70B 0.4720, LLAMA3-8B 0.1562.
  CTI-TAA (correct% / plausible%): GPT-4-turbo 52%/86%, GPT-3.5-turbo 44%/62%,
  Gemini-1.5 38%/74%, LLAMA3-70B 52%/80%, LLAMA3-8B 28%/36%.

## Limitations of this source

- Numbers were extracted via automated HTML-to-summary tooling from the arXiv HTML
  render, not manually cross-checked cell-by-cell against the PDF tables; treat as
  approximate pending manual verification.
- CTI-VSP (CVSS severity prediction) results were reported as Mean Absolute Deviation
  (lower is better) and are not directly comparable to the percentage/F1 metrics above;
  omitted from key claims to avoid a misleading comparison.
- Model list is from mid-2024-era releases (gpt-4-turbo, Gemini-1.5, LLAMA3); results
  may not generalize to newer model generations available as of 2026.

---

## Appendix — cycle-23 G2 re-verification (APPENDED; nothing above this line altered)

Re-verified 2026-07-30 (cycle 23) against `https://arxiv.org/html/2406.07599` and
`https://arxiv.org/html/2406.07599v3`. Two fetches with different prompts; both returned the
same Table 1 and the two agree cell for cell.

### PASS — every stored number is exact

All 25 stored cells (CTI-MCQ, CTI-RCM, CTI-ATE, CTI-TAA correct/plausible × 5 models) match
Table 1 verbatim. **This discharges the "Limitations of this source" note above, which warned
the numbers had never been cross-checked cell by cell.** Table 1 verbatim, caption
*"Table 1: Results of different LLMs on the benchmark datasets (bold indicates the best
performing model, lower is better for MAD)"*:

| Model | CTI-MCQ (Acc) | CTI-RCM (Acc) | CTI-VSP (MAD) | CTI-ATE (Macro-F1) | CTI-TAA (Acc) Correct | CTI-TAA (Acc) Plausible |
|---|---|---|---|---|---|---|
| ChatGPT-4 | 71.0 | 72.0 | 1.31 | 0.6388 | 52 | 86 |
| ChatGPT-3.5 | 54.1 | 67.2 | 1.57 | 0.3108 | 44 | 62 |
| Gemini-1.5 | 65.4 | 66.6 | 1.09 | 0.4612 | 38 | 74 |
| LLAMA3-70B | 65.7 | 65.9 | 1.83 | 0.4720 | 52 | 80 |
| LLAMA3-8B | 61.3 | 44.7 | 1.91 | 0.1562 | 28 | 36 |

Two refinements: the ATE column is **Macro-F1**, not plain F1; and the **CTI-VSP (MAD)** column
was never stored in this base (it is now, above — lower is better, not comparable with the
percentage/F1 columns, which is why the original collection omitted it).

The stored motivation quotation passed the exact-string check. Full sentence: *"While existing
benchmarks provide general evaluations of LLMs, there are no benchmarks that address the
practical and applied aspects of CTI-specific tasks."*

### FAIL — key claim 2's *interpretation* is not supported by the paper

**Correct and Plausible are NESTED, not disjoint.** Section 4.2, verbatim:

> "Based on these categories, we compute two types of accuracy: Correct Accuracy, which is the
> fraction of correct answers, and Plausible Accuracy, which is the fraction of correct and
> plausible answers combined."

Plausible Accuracy **contains** Correct Accuracy. So "the plausible rate is much higher than the
correct rate" is **true by construction** for every model and is not an empirical finding. The
empirical quantity is the **difference** — the plausible-but-not-correct share, derived by
subtraction and **not printed in the paper**: GPT-4-turbo **34**, GPT-3.5-turbo **18**,
Gemini-1.5 **36**, LLAMA3-70B **28**, LLAMA3-8B **8** percentage points.

**"Plausible" is not the paper's hallucination category.** Section 4.2, verbatim:

> "For the CTI-TAA task, we categorize the predictions into three types: correct answer (when the
> LLM accurately identifies the threat actor or one of its aliases), plausible answer (when the
> threat report lacks sufficient details to pinpoint the answer, but the LLM provides a plausible
> or related threat actor within a similar group), and incorrect answer (when the LLM misattributes
> the threat actor due to misjudgment, hallucination, or spurious correlation)."

"Plausible" is scoped to **underdetermined input** answered with a related actor from a similar
group; **misjudgment / hallucination / spurious correlation is the separate third bucket.** The
paper's own confident-but-wrong rate is therefore `100 − Plausible Accuracy` (derived, not
printed): GPT-4-turbo **14%**, GPT-3.5-turbo **38%**, Gemini-1.5 **26%**, LLAMA3-70B **20%**,
LLAMA3-8B **64%**.

Two further exact-string results: **`plausible-sounding` does NOT occur in this paper** (key
claim 2 and `index.json` present it in scare quotes as if quoting), and **no sentence in the
paper interprets the correct-versus-plausible gap** — the interpretation is entirely ours.

Judging procedure, verbatim: *"To further ensure accuracy, we manually verify each LLM response
to account for the multiple aliases that threat actors often use."* (§3.5) and, Appendix D,
*"we employed a graph-searching algorithm to determine the accuracy of model predictions."*

### What survives

CTIBench still supports a confident-but-wrong finding — the paper's *incorrect* bucket is
explicitly hallucination-inclusive and runs 14–64% — but **the number is not 86 vs 52, and the
gap is not evidence of overconfidence on its own.** Recorded as contradiction **`ctr-0002`**
against `attribution-confident-wrong-gap`. Nothing above this appendix has been deleted or
rewritten.

---

## CYCLE-28 INTERPRETATION CHECK (T3) — APPENDED, NOTHING ABOVE REMOVED OR REWRITTEN

This section is step (iii) of `ctr-0002`'s resolution path: the step that asked whether this
source's **other two** key claims — key claim 1 (task difficulty uneven) and key claim 3
(nothing exceeds ~72%) — carry an unstated interpretive gloss like the one cycle 23 found in
key claim 2. **Both do, and key claim 3 is false as written.** Contradiction `ctr-0006` is
filed against `ttp-attack-mapping-reliability`, whose supported candidate repeats the gloss.

Method: two fetches of `arxiv.org/html/2406.07599v3` and one of `arxiv.org/html/2406.07599`
(which resolves to v3), each instructed to quote verbatim or write ABSENT.

### 1. The paper contradicts itself on the CTI-ATE metric

Section 4.2 body text, PRESENT verbatim in two independent fetches:

> "We adopt the Micro-F1 score as the evaluation metric for the CTI-ATE task. Given that this
> task requires accurately extracting the relevant attack techniques from the provided text and
> mapping them to their corresponding MITRE ATT&CK technique IDs, the Micro-F1 score is suitable
> for capturing both precision and recall in a multi-label classification setting."

Table 1's column header, PRESENT verbatim in the same fetches and independently established at
cycle 23:

> "CTI-ATE (Macro-F1)"

Micro-F1 and Macro-F1 are different quantities and diverge substantially in multi-label settings
with a long tail; the dataset "comprises 397 unique attack techniques" over 60 malware instances.
**So 0.6388 is a number whose metric the paper itself does not settle.** This is a defect in the
source, not in this base, and it is not resolvable from the paper.

### 2. The cross-task difficulty comparison is ours, not the paper's

The complete metrics paragraph, verbatim:

> "We use accuracy to evaluate the CTI-MCQ and CTT-RCM tasks, as both tasks are equivalent to
> multi-class classification. For the CTI-VSP task, we compute the mean absolute deviation (MAD)
> between the CVSS v3.1 scores of the ground truth and the model's predictions. Although we ask
> the model to predict a vector string, the CVSS score can be deterministically derived from it.
> We utilize the Python library cvss to compute the CVSS score (a numerical value in the range of
> 0-10 that determines the overall severity of a vulnerability) from the predicted string,
> ensuring that any potential errors from the LLM performing the computation are eliminated. This
> approach focuses solely on assessing the LLM's reasoning ability regarding vulnerability. We
> adopt the Micro-F1 score as the evaluation metric for the CTI-ATE task. Given that this task
> requires accurately extracting the relevant attack techniques from the provided text and
> mapping them to their corresponding MITRE ATT&CK technique IDs, the Micro-F1 score is suitable
> for capturing both precision and recall in a multi-label classification setting."

A multi-class classification **accuracy** with a non-trivial chance baseline and a multi-label
**F1** over 397 technique IDs with a near-zero chance baseline cannot be differenced. Exact-string
checks: `task difficulty` **ABSENT**, `most challenging` **ABSENT**; a fetch asked for *every*
sentence comparing performance BETWEEN tasks answered **ABSENT**. The nearest sentence the paper
offers is a cross-**model** observation:

> "ChatGPT-4 significantly outperforms other models on the CTI-ATE task, underscoring the
> complexity of the task."

So *ATT&CK-mapping-is-hard* has a weak textual anchor in the paper's own words; the **quantified
cross-task gap has none** and must not be stated as a measured difference.

### 3. Key claim 3 is false against the paper's own printed table

Key claim 3 above says GPT-4-turbo "does not exceed ~72% on any task"; `index.json`
key_claims[2] generalises it to all five models. Table 1's **CTI-TAA Plausible** column reads
**ChatGPT-4 86, LLAMA3-70B 80, Gemini-1.5 74** — three models above 72 on a printed task
accuracy. The exact string `no model` is **ABSENT** (three fetches, two URL forms). The claim
holds only under an unstated restriction to CTI-MCQ, CTI-RCM and CTI-TAA-**Correct**, and it is
mutually inconsistent with key claim 2 of this same file, which foregrounds the 86% figure.

### 4. The ATT&CK correctness rule is never stated

Asked how a predicted technique is counted as correct (exact technique-ID match, parent
technique, sub-technique, partial credit), two fetches returned **ABSENT**. Same gap as
src-0003's unstated IoC matching rule (carry-forward [33]); bears directly on [34].

### 5. Provenance — v2 has no CTI-ATE task at all

A fetch of `arxiv.org/html/2406.07599v2` returns **four** tasks (CTI-MCQ, CTI-RCM, CTI-VSP,
CTI-TAA), **no F1 metric anywhere**, and a Table 1 with **no ATE column**. CTI-ATE was added in
v3. **Always fetch v3 or the latest render — a v2 fetch returns spurious ABSENTs.**

### 6. Re-confirmed a third time, and sample counts captured for the first time

ChatGPT-4 row of Table 1: **71.0 / 72.0 / 1.31 / 0.6388 / 52 / 86**. Caption: "Results of
different LLMs on the benchmark datasets (bold indicates the best performing model, lower is
better for MAD)". Sample counts, verbatim, never previously stored in this base: CTI-MCQ
"2500 questions"; CTI-RCM "1,000 vulnerabilities"; CTI-VSP "1,000 vulnerability descriptions";
CTI-ATE "60 instances of malware (30 post-2024 cutoff, 30 pre-2024)" with "397 unique attack
techniques"; CTI-TAA "50 threat reports".

## Cycle 86 addendum (G2 re-verification, appended not substituted)

Fetched `https://arxiv.org/abs/2406.07599` on 2026-08-04 for authorship provenance, which
this base had never recorded despite `ttp-attack-mapping-reliability`
`candidate_resolutions[1]` resting a claim on it since cycle 16.

- **Author field, verbatim:** 'Md Tanvirul Alam, Dipkamal Bhusal, Le Nguyen, Nidhi Rastogi'
- **Title, verbatim:** 'CTIBench: A Benchmark for Evaluating LLMs in Cyber Threat Intelligence'
- **Subjects, verbatim:** 'Cryptography and Security (cs.CR); Artificial Intelligence (cs.AI)'
- **Comments field:** ABSENT. **Journal reference field:** ABSENT. No venue is recorded for
  this source, on this page, as of cycle 86.
- **Institutional affiliations:** ABSENT. The fetch was asked directly for any sentence
  stating them and returned ABSENT. arXiv abstract pages do not print affiliations.

**What this establishes and what it does not.** The four names above are disjoint from the
eight `src-0007` author names stored in `state/knowledge/src-0007.md`, so the two teams
share no author. It does **not** establish institutional non-overlap, which is what the
word 'unaffiliated' in that candidate asserts. One render family only. See `ctr-0045`.

## Cycle 87 addendum (G2 re-verification, appended not substituted)

**Second render family, at last, for the CTI-TAA numbers.** Every prior pull of Table 1 came
from `arxiv.org/html` — two at cycle 23 (`.../2406.07599` and `.../2406.07599v3`, different
prompts), three at cycle 28 (v3 twice, bare once). That is **one render family, five times**.
On 2026-08-04 the paper was pulled through `r.jina.ai` over `https://arxiv.org/pdf/2406.07599`,
a genuinely different family; the render self-reported **COMPLETE**, last visible heading
`G Compute Cost`.

Caption reproduces verbatim: *"Results of different LLMs on the benchmark datasets (bold
indicates the best performing model, lower is better for MAD)"*. Column headers reproduce as
`CTI-MCQ (Acc)`, `CTI-RCM (Acc)`, `CTI-VSP (MAD)`, `CTI-ATE (Macro-F1)`, and `CTI-TAA (Acc)`
spanning `Correct` / `Plausible`. **All ten CTI-TAA cells match the stored values exactly:**

| Model | Correct | Plausible |
|---|---|---|
| ChatGPT-4 | 52 | 86 |
| ChatGPT-3.5 | 44 | 62 |
| Gemini-1.5 | 38 | 74 |
| LLAMA3-70B | 52 | 80 |
| LLAMA3-8B | 28 | 36 |

**New negative.** Asked directly whether the paper anywhere prints a numeric incorrect-answer
or misattribution rate for CTI-TAA — or any of 14, 38, 26, 20, 64 presented as such — this
complete render returned **ABSENT**. The standing parenthetical that the five misattribution
rates are *derived by subtraction and not printed* now has a positive test behind it for the
first time.

**Limits, charged not credited.** (a) That ABSENT is **one-form**: no `arxiv.org/html` pull was
ever asked it, so the numbers are two-form and the absence is not. (b) This fetch was scoped to
CTI-TAA, so **fifteen of the twenty-five cells** verified at cycle 23 (MCQ, RCM, VSP, ATE)
remain single-render-family. See `ctr-0046`.

## Cycle 95 addendum — the authorship record is now two-form

**What this is.** A G2 re-verification of the conclusion **cycle 86 entered** — `key_claims[7]`
of this source in `index.json` — run at cycle 95. Cycle 86 read `arxiv.org/abs/2406.07599` in
**one render family** and charged that limit **against itself**, not against a successor. This
cycle discharges it.

**The check.** The same URL through the second render family,
`https://r.jina.ai/https://arxiv.org/abs/2406.07599`, fetched 2026-08-05, with a query
demanding every field verbatim, `ABSENT` for anything not present, and an **enumeration** of
every affiliation occurrence with an explicit instruction not to answer *several* or *multiple*
(the query discipline of `ctr-0049` and cycles 93–94).

**Result: the two render families agree exactly on all six fields, with no disagreement of any
kind.**

| field | arXiv abstract page (cycle 86) | `r.jina.ai` (cycle 95) |
|---|---|---|
| Authors | `Md Tanvirul Alam, Dipkamal Bhusal, Le Nguyen, Nidhi Rastogi` | identical; count reported as **4** |
| Title | `CTIBench: A Benchmark for Evaluating LLMs in Cyber Threat Intelligence` | identical |
| Subjects | `Cryptography and Security (cs.CR); Artificial Intelligence (cs.AI)` | identical |
| Comments | ABSENT | ABSENT |
| Journal reference | ABSENT | ABSENT |
| Institutional affiliation | ABSENT | ABSENT (explicit enumeration-of-none) |

The affiliation answer was an **enumeration of none**, not a summary refusal, so it is a
**complete answer** and not the partial-decline shape cycle 94 recorded.

**Consequence 1, load-bearing.** The author-name **disjointness** between this source and
`src-0007` is now established in **two independent render families**. `src-0007` is **not** the
CTIBench team measuring its own benchmark again, and `ttp-attack-mapping-reliability`
`candidate_resolutions[1]` claim (a) — INDEPENDENCE — **survives** this second check.

**Consequence 2, cutting the other way.** **`ctr-0045` remains open and is strengthened, not
weakened.** The word *"unaffiliated"* asserts **institutional** non-overlap, and institutional
affiliation is now confirmed **absent from the page in both render families**. *Absence of
evidence in two forms is still absence of evidence, not evidence of non-affiliation* — a second
render cannot supply a fact the page does not carry, and no number of renders of this URL ever
can. **The institutional reading must still not be cited as established;** only disjoint author
names are.

**Limits, charged and not credited.** (a) This re-read the **same URL** in a second form and
consulted **no different artefact** — no published version, no PDF, no institutional page — so
the affiliation question is **unresolved rather than closed**, and a successor wanting
institutional non-overlap must fetch a **different artefact**, not another render of this one.
(b) This source **still carries no venue** on this base's record after two independent checks of
the abstract page, both returning Comments and Journal-reference ABSENT. It remains an
**unreviewed preprint** on the record this base holds, while `src-0007` was recorded at cycle 53
as an accepted **TMLR** paper — the two sit on opposite sides of several comparisons in this
graph, and that asymmetry is now **twice-confirmed** rather than once-observed.

See `ctr-0045`, `index.json` `src-0002` `key_claims[9]`, and `logs/cycle-095.md`.
