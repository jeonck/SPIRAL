---
url: https://www.sentinelone.com/labs/from-narrative-to-knowledge-graph-llm-driven-information-extraction-in-cyber-threat-intelligence/
title: "From Narrative to Knowledge Graph | LLM-Driven Information Extraction in Cyber Threat Intelligence"
type: article
accessed: 2026-07-30
added_cycle: 25
---

## Why this source was collected (cycle 25, T3)

The issue `consistency-calibration-as-failure-mode` had been stuck at score 2 since
cycle 3 for exactly one reason: both of its supported candidate_resolutions cited
src-0001 and nothing else. Cycle 16's T2 scope ruling (endorsed at cycles 19 and 22)
held that src-0013/0014/0015/0016 do not count as corroboration because none of them
measures a **CTI** task. This cycle's job was to find a **CTI-task** measurement of
repeat-query consistency or of confidence calibration by a team unrelated to src-0001's.

This source is that measurement for the **consistency** half only. It is **not** an
ECE/Brier calibration measurement — see the scope limits below.

## Bibliographic facts (verbatim from the page)

- Title: "From Narrative to Knowledge Graph | LLM-Driven Information Extraction in Cyber Threat Intelligence"
- Authors: Aleksandar Milenkoski & Razvan Gabriel Cirstea
- Publication date: March 9, 2026
- Publisher: SentinelOne (SentinelLabs)

Independence from src-0001 (Mezzi, Massacci, Tuma — Vrije Universiteit Amsterdam /
University of Trento, ARES 2025): different authors, different institution type
(industry threat-research lab vs academia), different corpus, different model
generation, and the page does not cite src-0001. There is no shared author.

## What is measured, and on what (all quotations verbatim)

Task — selective IOC extraction from narrative threat reports plus contextual-attribute
assignment and playbook reconstruction:

- "The selective extraction of network-related atomic IOCs (domains and IPs) focuses on
  indicators that have played an active role in the adversary activity described in the
  input report, encompassing both attacker-owned assets and compromised external systems
  deliberately used for malicious purposes."
- "The selective extraction of hashes focuses on those associated with malicious or
  attacker-used software components that have been deployed within victim environments,
  including both custom malware and publicly available tools used for offensive purposes."
- "Extracts distinct threat actor actions, represented by `Step` entities, and groups
  them into one or more `Playbook` entities."

Corpus:

- "The reported results are preliminary and based on a limited ground truth dataset
  comprising 343 atomic IOCs and 1859 labeled IOC contextual attribute instances."

Models:

- "An evaluation study of several off-the-shelf, general-purpose language models from
  OpenAI and Anthropic — GPT-4.1, GPT-5, GPT-5.2, Claude Sonnet 4.5, and Claude Opus 4.5
  — used within the `Infrastructure`, `Executables`, and `Playbook Extractor` without any
  additional fine-tuning or task-specific adaptation."

Note the model list: **five current-generation models, none of which overlaps src-0001's
(gpt4o, gemini-1.5-pro-latest, mistral-large-2), src-0002's or src-0005's lists.** This
source therefore extends the phenomenon to a newer model generation on CTI material but
does **not** close this issue's open question about replicating on those specific lists.

## The consistency measurement (this is the load-bearing part)

Repetition protocol, verbatim:

- "To account for the inherent non-determinism of LLMs and to provide statistically
  reliable results, all reported metric values were obtained through repeated executions
  of each evaluation experiment until the point estimates reached a 95% confidence level
  with a relative precision of less than 5%."

Note the shape of this: like src-0001 it repeats an **identical** input and treats the
spread as the quantity of interest, but it fixes a **precision target** rather than a
run count (src-0001 fixed ten re-prompts and bootstrapped CIs). No explicit repetition
count is printed anywhere on the page — recorded as ABSENT, not inferred.

Two decision-consistency metrics, both defined verbatim on the page:

- P_o: "The proportion of extracted IOCs for which, across repeated runs under identical
  conditions, the LLM assigned two or more different values for the same attribute,
  alternating among the values that the expert annotators defined as valid."
- D̄_o ("average mode-based observed disagreement"): "For the subset of IOCs with P_o < 1,
  the average proportion of the LLM-assigned attribute values across the repeated runs
  that differ from the dominant (mode) value."

Interpretation, verbatim:

- "Higher percentages of IOCs with P_o < 1 indicate greater fluctuation in how the LLM
  interprets ambiguous evidence, while higher D̄_o values show that, in cases where the
  model switches between valid attribute values, its decisions are more evenly
  distributed among the alternatives rather than converging on a single dominant
  interpretation."

The measurement is scoped to the two contextual attributes on which expert annotators
frequently accepted more than one value ("`is_compromised`" and "`attack_stage`") — i.e.
it deliberately targets **ambiguous evidence**, which is a narrower and easier-to-fluctuate
setting than src-0001's whole-task re-prompting. Do not read it as an all-attributes
instability rate.

## Abstention (confidence-adjacent, NOT calibration)

- "The value class `None` provides an explicit abstention option for value assignment,
  allowing the LLM to assign `None` to an IOC contextual attribute when evidence is
  insufficient, rather than outputting a concrete value."
- "False Discovery Rate (FDR): the proportion of `None` assignments that were unwarranted"
- "False Negative Rate (FNR), the proportion of instances that should have abstained but
  did not"

This is the **second** abstention/restraint construct in this base (after src-0014's
selective-prediction coverage and src-0015's action-threshold FP rate). It is **not**
ECE/Brier confidence calibration and must never be pooled or averaged with src-0001's or
src-0013's ECE figures — the same rule already recorded for src-0015.

## THE CRITICAL LIMITATION: NO NUMBER ON THIS PAGE IS RECOVERABLE

Every quantitative artefact on the page is an **image**. Two independent fetches on
2026-07-30, the second one asking cell by cell and instructed to answer "IMAGE - CANNOT
READ" rather than infer, returned exactly that for all of:

- the table captioned "LLM decision consistency" (five models x {is_compromised,
  attack_stage} x {% of IOCs with P_o < 1, D̄_o});
- the chart "Abstention error rates" (FDR and FNR per model);
- the charts "Selective IOC extraction performance" and "Value assignment performance".

So this source supports the **direction and the existence** of repeat-run decision
inconsistency on a CTI extraction task, in the authors' own words, and supplies **zero
citable magnitudes**. No cycle may quote, estimate, rank or compare numbers from it
until someone reads those images. This is the same defect class as src-0003's Figure 6
(the 76/72/86 baseline F1 values) — see logs/cycle-022.md.

## Other stated limitations (verbatim)

- "The purpose of this evaluation is not to provide conclusive performance comparisons
  but to demonstrate the feasibility of using LLMs for information extraction from threat
  intelligence narratives."
- "The reported results are preliminary and based on a limited ground truth dataset..."
  (quoted in full above)
- "LLMs can effectively automate information extraction from CTI reports, delivering
  substantial speed gains over manual processing. However, these reports vary widely in
  structure, terminology, and level of evidentiary detail, and the contextual cues needed
  to support LLM inference for a given extraction task may be implicit, inconsistent, or
  absent."
- "LLMs' use of generative reasoning for extraction, combined with ambiguity and uneven
  detail in many reports, can introduce inconsistencies or omissions in the reconstructed
  structures, reducing overall coherence."

## Provenance cautions

- Vendor blog post, not peer reviewed, no DOI, no venue. SentinelOne sells threat
  intelligence products, so a post demonstrating a SentinelLabs extraction pipeline has a
  commercial interest — though note the direction of the interest differs from src-0016's:
  Snyk's baseline was its own product, whereas here the finding (their own pipeline's
  models fluctuate) is unfavourable-to-neutral for the publisher, which slightly
  strengthens the consistency finding rather than weakening it.
- The authors state the results are "preliminary".
- Ground truth is manually annotated by the authors' own expert annotators; no
  inter-annotator agreement statistic is printed on the page (checked: ABSENT).

---

## CYCLE-28 G2 VERIFICATION AND CORRECTION — APPENDED, NOTHING ABOVE REMOVED OR REWRITTEN

First verification of this source since it was added at cycle 25; it was the only source in the
base that had never been checked. Contradiction `ctr-0005` is filed. Method: two fetches — the
article URL, and a `?print=1` rendering, the second instructed to quote every sentence containing
a digit and to write "IMAGE - CANNOT READ" rather than infer.

### Positive half — every stored quotation is exact

All stored verbatim quotations re-verified **PRESENT and exact**: the 343/1859 ground-truth
sentence; the 95%-confidence repeated-execution protocol; the `P_o` definition; the average
mode-based observed disagreement `D̄_o` definition; the "value class `None`" abstention sentence;
the FDR and FNR definitions; the "not to provide conclusive performance comparisons" limitation;
the five model names (GPT-4.1, GPT-5, GPT-5.2, Claude Sonnet 4.5, Claude Opus 4.5); the byline and
the date.

The four **performance** artefacts are confirmed **IMAGE - CANNOT READ for a third independent
time**. The prohibition on quoting, estimating, ranking or comparing the *consistency*,
*abstention* and *extraction-performance* magnitudes **stands in full**, and no cycle should
re-attempt them by fetch. Only OCR would recover them.

### Corrective half — "no number is recoverable" is wrong, and too broad

`index.json` key_claims[1] and the corresponding section above say **"NO NUMBER ON THE PAGE IS
RECOVERABLE"**, "Every quantitative artefact is an IMAGE", and "supplies **ZERO citable
magnitudes**". The page carries these in **plain body text**, verbatim:

> "In all cases, the use of LLMs substantially reduced report processing time compared with human
> analysts, whose average was 41 minutes per report."

> "On average, the extractors required about 3.3 minutes per report, corresponding to an aggregate
> speed-up of more than 18 times."

> "Even the slowest LLM-based setup processed reports approximately 6 times faster than the human
> baseline, while the fastest reduced average processing time by more than 97% relative to the
> human baseline."

> "The analysis is based on 17 individual metrics, each expressed as a ratio between 0 and 1
> representing the proportion of structural or semantic elements (such as data model entities,
> links and their typed relationships, MITRE ATT&CK mappings, and IOC associations) that satisfy
> defined validation rules or external references, out of all instances evaluated for the
> respective metric."

> "For the Structural Integrity and ATT&CK Mapping Validity categories, higher values indicate
> greater structural and semantic correctness, reaching 1.0 for fully valid results."

The stored claim is also **internally inconsistent with key_claims[0] of this same source**, which
already cites "343 atomic IOCs and 1859 labeled IOC contextual attribute instances" — numbers on
the page.

### Two substantive things the over-broad ban locked out

**(a) Temperature was set to 0.** Verbatim:

> "Where applicable, the reasoning mode for the GPT models was set to High, the Claude models were
> configured with a thinking budget of 16000 tokens to allow for extended reasoning, and the LLM
> temperature parameter was set to 0 to minimize randomness and reduce non-deterministic
> behavior."

The measured repeat-run decision inconsistency is therefore **residual non-determinism under a
determinism setting** — the same design property cycle 25 found strengthened src-0001's
consistency result ("temperature=0 and the same seed"). **Two independent teams now report
CTI-task output instability at temperature 0.** This strengthens the *consistency* half of
`consistency-calibration-as-failure-mode` and was locked out by an over-broad hedge.

**(b) A human-analyst baseline exists here.** 41 minutes per report against about 3.3 minutes.
It is a **throughput** baseline, **not** an accuracy baseline, so it does **not** answer
`ttp-attack-mapping-reliability`'s sixteen-cycle `open_question[1]`, which asks for a
human-analyst **F1** on ATT&CK extraction. But it is the only human-versus-LLM comparison of any
kind anywhere in this base, and no cycle knew it was here.

### Also corrected — ten figures, not four

The stored text refers to "its four images". The page carries **ten** figures, six of them
quantitative. Alt text / captions, verbatim: "CTI information extraction workflow"; "The data
models for the Infrastructure and Executables Extractor (simplified version)"; "The data model for
the Playbook Extractor (simplified version)"; "LLM decision consistency"; "Selective IOC
extraction performance"; "Human vs. LLMs: Time efficiency in report processing"; "Value assignment
performance"; "Abstention error rates"; "Prediction and error diversity (GPT‑4.1 and Claude
Sonnet 4.5)"; "LLM Performance in Playbook and knowledge graph assembly". The last two were
**unknown to this base**.

### Methodological by-product — fourth instance of carry-forward [38]

The **first** fetch this cycle returned the `P_o` and `D̄_o` definitions as **ABSENT**; the
**second URL form** returned both **PRESENT and exact**. A single fetch's ABSENT is still not
evidence of absence.

---

## CYCLE-52 G2 RE-VERIFICATION — APPENDED, NOTHING ABOVE REMOVED OR REWRITTEN

Second verification of this source (previous: cycle 28). It was the **stalest source in the
base** at 24 cycles and had been top of the staleness list for four consecutive cycles without
being picked. Method: one verification-mode fetch of the article URL, numbered items, each
answered PRESENT / ABSENT / IMAGE - CANNOT READ, with cycle 51's paragraph-first-word-to-last-word
hardening applied to item 8.

### VERDICT: PASSED. Every stored string re-verified PRESENT and exact.

Re-confirmed verbatim, one fetch each, all PRESENT:

- "The reported results are preliminary and based on a limited ground truth dataset comprising
  343 atomic IOCs and 1859 labeled IOC contextual attribute instances."
- "all reported metric values were obtained through repeated executions of each evaluation
  experiment until the point estimates reached a 95% confidence level with a relative precision
  of less than 5%."
- "In all cases, the use of LLMs substantially reduced report processing time compared with
  human analysts, whose average was 41 minutes per report."
- "On average, the extractors required about 3.3 minutes per report, corresponding to an
  aggregate speed-up of more than 18 times."
- "the LLM temperature parameter was set to 0 to minimize randomness and reduce
  non-deterministic behavior."
- "Even the slowest LLM-based setup processed reports approximately 6 times faster than the
  human baseline, while the fastest reduced average processing time by more than 97% relative
  to the human baseline."
- "The analysis is based on 17 individual metrics, each expressed as a ratio between 0 and 1
  representing the proportion of structural or semantic elements"
- The five-model list, verbatim: "In the following sections, we present the results of an
  evaluation study of several off-the-shelf, general-purpose language models from OpenAI and
  Anthropic — GPT-4.1, GPT-5, GPT-5.2, Claude Sonnet 4.5, and Claude Opus 4.5"
- Byline and date: "Aleksandar Milenkoski & Razvan Gabriel Cirstea / March 9, 2026"

So the cycle-28 corrective half (the numbers that ARE in plain body text) is **confirmed at a
second cycle**, and the bibliographic facts are intact.

### The image-lock is confirmed for a FOURTH independent time

The table captioned "LLM decision consistency" is present on the page and its numeric cells
returned **IMAGE - CANNOT READ**. The prohibition on quoting, estimating, ranking or comparing
the consistency / abstention / extraction-performance magnitudes **stands in full**, now on four
independent fetches across three cycles (25, 28, 52). No cycle should re-attempt it by fetch;
only OCR would recover it.

### ONE PREVIOUSLY UNRECORDED SENTENCE, surfaced by the paragraph-first-to-last-word ask

Item 8 asked for the ENTIRE paragraph containing "41 minutes per report", first word to last
word. Cycle 28 recorded the paragraph's first three sentences. It has a **fourth**, which no
cycle has ever recorded, verbatim:

> "These results highlight the considerable time-efficiency gains achieved by using LLMs for CTI
> information extraction compared with traditional human workflows, though with accompanying
> trade-offs in extraction completeness and correctness."

**This does NOT contradict anything in the state and no contradiction entry is opened for it.**
Cycle 28 already recorded, correctly, that the 41-vs-3.3-minute comparison is a **throughput**
baseline and **not** an accuracy baseline. The omitted sentence is the source stating that
limitation in its own words, which corroborates the state's reading rather than conflicting with
it. It is recorded here because it is the source's own explicit acknowledgement that the speed-up
is bought at a cost in completeness and correctness — the one sentence a future cycle citing the
"18x speed-up" figure would most need, and it was invisible to this base for 27 cycles.

**Methodological note.** This is the third consecutive cycle in which the paragraph-level form of
rule (iv) has surfaced material no string-level check would have found (cycle 50 extended the rule
to the whole paragraph, cycle 51 produced ctr-0019 with it, cycle 52 produces this). Unlike
cycles 50 and 51, here the omitted material was **benign** — which is itself worth recording, as
evidence that the hardening is not merely a defect-finder that always fires.

## Cycle 75 — T3 (APPENDED; nothing above deleted or rewritten)

Discharges **ctr-0035 step (ii)** in the same cycle that opened ctr-0035, so it does not join the
repair backlog. One verbatim-mode fetch of the page, every limb given an explicit ABSENT option,
and a separate truncation question (answered: not truncated, ends at the footer).

### 1. The two decision-consistency metrics, defined verbatim for the first time

The base has carried only a paraphrase of these since cycle 25. Rendering note: the page sets the
subscript as a Unicode subscript `o`, transcribed here as `P_o` / `D-bar_o`.

> IOCs with P_o < 1: The proportion of extracted IOCs for which, across repeated runs under
> identical conditions, the LLM assigned two or more different values for the same attribute,
> alternating among the values that the expert annotators defined as valid (yielding observed
> agreement P_o < 1 across runs).

> Average mode-based observed disagreement D-bar_o: For the subset of IOCs with P_o < 1, the
> average proportion of the LLM-assigned attribute values across the repeated runs that differ
> from the dominant (mode) value.

**The load-bearing clause is "alternating among the values that the expert annotators defined as
valid."** This source's headline consistency statistic counts alternation *confined to the
accepted-answer set*. It can be strictly positive while the pipeline is correct on every run.
It is therefore **not** the same measurand as src-0001's run-to-run precision/recall CI widths,
which measure variation in correctness. **ctr-0035** is opened on this point.

### 2. The repeat protocol is a stopping rule, not a fixed N

> To account for the inherent non-determinism of LLMs and to provide statistically reliable
> results, all reported metric values were obtained through repeated executions of each evaluation
> experiment until the point estimates reached a 95% confidence level with a relative precision of
> less than 5%.

No fixed repetition count appears anywhere. Contrast src-0001's fixed ten re-prompts with a fixed
seed. **Consequence:** the standing rule that no magnitude from this source may be compared with
src-0001's CI widths of 0.02 / 0.06 now rests on *two* independent grounds — different measurand
and different estimator — not only on the contingent fact that the numbers are locked in images.
*Even if those images are ever read, the comparison remains forbidden.*

### 3. This source elicits no model-expressed confidence of any kind

`logit`, `log probability`, `token probability`, `verbalized` — **all ABSENT**. Asked for every
sentence containing `confidence`, the page yields none in which a model states or emits a
confidence value; the word occurs only in the statistical sense ("95% confidence level") and in an
evidence-grading phrase ("Evidence confidence levels"). So src-0018 sits on **no point of the
confidence-elicitation axis** and must never be pooled with a calibration source. Its uncertainty
constructs are behavioural: abstention, and cross-run decision consistency. This answers the
src-0018 limb of `consistency-calibration-as-failure-mode` open_questions[8].

### 4. The abstention construct, verbatim

> By enabling abstention, a value such as `None` reduces incorrect assignments, communicates
> uncertainty, and allows downstream consumers to defer, escalate, or exclude that data point.

> The abstention option requires careful consideration because it trades correctness, including
> accuracy and precision, against coverage.

The second sentence states the coverage/correctness trade-off the base had only inferred.

### 5. Not disturbed, not re-tested

Cycle 39's verbatim recovery that "the LLM temperature parameter was set to 0 to minimize
randomness and reduce non-deterministic behavior" stands and was **not** re-tested this cycle. No
seed is stated by this source, where src-0001 fixes one — recorded as a difference, not a defect.
The four performance artefacts remain IMAGE, CANNOT READ; the three-strikes no-refetch rule on them
STANDS. This cycle did not re-attempt them and no successor should.

### 6. A method warning earned this cycle

A web search returned, in prose adjacent to a result for this very page:

> Generative enrichment is the right place for confidence scoring, with the model prompted to
> return a per-field confidence score between 0 and 1, representing genuine epistemic uncertainty
> given the source document content.

Had that been credited to src-0018 it would have made this a **verbalized-confidence source** and
inverted §3 entirely. The direct fetch shows it is not in this page. **A search-engine summary is
not a source read, even when the source's own URL is sitting in the result list.**

---

## Cycle 80 — T3: two-render body-text sweep, and a NEGATIVE that is deductive rather than merely not-found

Discharging `ctr-0035` repair step (iii), which cycles 76, 77 and 78 each named as the
highest-value open repair in the graph and none spent. **Nothing above is altered.**

**THE QUESTION.** Does src-0018 report *any* quantity measuring variation in the
**correctness** of its outputs across repeated runs — which would restore a like-for-like
pairing with src-0001's bootstrapped precision/recall/F1 CI widths?

**THE ANSWER IS NO, ON TWO RENDER FAMILIES.** The body text was swept at cycle 80 on the
direct `sentinelone.com` URL and again through the `r.jina.ai` proxy, a genuinely different
render family. Each fetch was instructed to write ABSENT rather than infer, and told not to
echo back a supplied string it had not found.

- Both forms returned **the same two consistency metrics and no third** — `IOCs with Pₒ < 1`
  and `Average mode-based observed disagreement D̄ₒ` — both confined by their own printed
  definitions (stored above, cycle 75) to alternation among values the **expert annotators
  accepted as valid**.
- `variance`, `standard deviation`, `error bar` and `stability` are **ABSENT on both forms**.
- The **number** of repeated executions performed is disclosed on **neither** form.

**ONE NEW VERBATIM SENTENCE, recovered on the proxy render and held nowhere in this base
before.** It settles the interpretation in the author team's own words rather than by this
base's reading of a definition:

> "higher percentages of `IOCs with Pₒ < 1` indicate greater fluctuation in how the LLM
> interprets ambiguous evidence"

**Fluctuation in the interpretation of ambiguous evidence is not variation in correctness**,
and that is SentinelLabs saying so.

**THE DEDUCTIVE POINT — new at cycle 80, and stronger than the absence itself.** src-0018
**could not supply the missing quantity from its reported numbers even in principle.** Its
stopping rule (stored above) is

> "all reported metric values were obtained through repeated executions of each evaluation
> experiment until the point estimates reached a 95% confidence level with a relative
> precision of less than 5%."

That is a **variable-`n` rule that halts when the interval around the point estimate has
become narrow enough**. It therefore pins the reported interval width **at the threshold by
construction**, so that width carries no information about the underlying run-to-run spread;
and because an interval half-width scales as spread ÷ √n, recovering the spread requires `n`,
which is undisclosed on both renders.

**The contrast with src-0001 is structural, not a difference in reporting diligence.**
src-0001's consistency quantity is a **fixed-`n`** bootstrapped CI over exactly 10 re-prompts,
and a fixed-`n` interval width *does* carry spread information. src-0018's, by design, cannot.

**CONSEQUENCE FOR ANY CYCLE PAIRING THE TWO SOURCES:** they are **two-source for the
existence** of run-to-run output instability on CTI material at temperature 0, and
**single-source (src-0001 alone) for instability in correctness**. Written positively into
`consistency-calibration-as-failure-mode` `candidate_resolutions[12]`; `ctr-0035` is
**RESOLVED at cycle 80**.

**WHAT THIS SWEEP DID NOT TOUCH.** The image artefacts, which remain unreadable by this
agent's tooling and under the standing three-strikes no-refetch rule. **Cycle 80 attempted no
image.**

