---
url: https://gptzero.me/news/neurips/
title: "GPTZero finds 100 new hallucinations in NeurIPS 2025 accepted papers (GPTZero News)"
type: report
accessed: 2026-08-06
added_cycle: 114
---

# Summary

**GPTZero's own public NeurIPS 2025 report** — the vendor-primary artefact this base has been
missing since cycle 101. Published **21 January 2026**. Fetched in **both render families** on
2026-08-06 by cycle 114 (a T3).

**HOW THE URL WAS OBTAINED, AND IT WAS NOT GUESSED.** `src-0038` (Ansari, arXiv 2602.05930) states
that it used *"the complete dataset of 100 verified hallucinations as documented in GPTZero's
public report [1]"*, and `src-0038.md` recorded that **this base had never seen that report**. The
lead was carried forward by cycle 103 expressly so it would not be lost and was still uncollected
ten cycles later. Cycle 114 resolved reference `[1]` out of the paper's **own bibliography** via
the `r.jina.ai` render of the arXiv PDF, which returned it verbatim:

> "[1] GPTZero. GPTZero finds 100 new hallucinations in NeurIPS 2025 accepted papers.
> https://gptzero.me/ news/neurips/ , 2026. Accessed: 2026-02-05."

The space inside `https://gptzero.me/ news/neurips/` is a **line-break artefact of the PDF
render**, not part of the URL; the address resolves as `https://gptzero.me/news/neurips/` and was
fetched successfully twice.

**WHY IT WAS COLLECTED, AND BY WHICH ISSUE'S CYCLE.** Cycle 114 was a T3 on
`institutional-incident-real-world-impact`, whose queue entry named this artefact as lead (1)
against that issue's **independence bar**. **It does not serve that issue** (see key claim 5 and
Limitations). It serves `incident-evidence-vendor-concentration`, where it discharges one of
`src-0038`'s self-charged limits and opens `ctr-0061`. The cycle records that redirection plainly
rather than presenting the source as having done the job it was fetched for.

# Key claims

1. **THE VENDOR-PRIMARY ARTEFACT, NOW HELD.** This is the document `src-0038` is downstream of.
   The base no longer knows GPTZero's NeurIPS sweep only at one remove. Publication date on the
   page: **"Jan 21, 2026"** (direct render) and **"Published Time: 2026-01-21T13:57:05.000Z"**
   (`r.jina.ai` render).

2. **THE 4,841 OF 5,290 DENOMINATOR PAIR IS CONFIRMED AT THE VENDOR, BOTH RENDERS —
   `src-0038`'s PROVISIONALITY LIMIT IS DISCHARGED.** `src-0038.md` charged against itself that
   the pair was **one-form** (its PDF render only) and *"should be treated as provisional until
   re-rendered"*, and separately that the base held it only at second hand. **Both defects are
   cured.** Direct render, unprompted:

   > "After scanning 4841 papers accepted by the equally prestigious Conference on Neural
   > Information Processing Systems (NeurIPS), we discovered 100s of hallucinated citations missed
   > by the 3+ reviewers who evaluated each paper."

   `r.jina.ai` render, unprompted:

   > "GPTZero's analysis of 4841 of the 5290 papers accepted by NeurIPS 2025 indicates noticeable
   > traces of AI authorship and hundreds of vibe citations."

   The renders **agree on 4841**. **5290** appears on the `r.jina.ai` render here and in
   `src-0038`'s PDF text, so it is confirmed across **two different artefacts** though only one
   render of this page.

3. **THE HUMAN-EXPERT VERIFICATION STEP, CONFIRMED AT THE VENDOR — character-identical and
   unprompted on BOTH renders:**

   > "As always, each of the hallucinations presented here has been verified by a human expert."

   This corroborates at the vendor what cycle 101 took at one remove from `src-0038` and what
   cycle 90 established for the EY instalment. **Note "As always"**: that is the vendor asserting
   the step is standing practice across its investigations. It is the **vendor's own
   characterisation** and this base has verified it only for the instalments it holds.

4. **A DISTINCT-PAPER COUNT DISAGREEMENT WITH `src-0038` — ENTERED AS A DISAGREEMENT, NOT AS A
   COUNT. See `ctr-0061`.** Direct render, asked unprompted with no number supplied:

   > "Below, we uncover 100 confirmed hallucinations in the table below, spanning over 51 NeurIPS
   > papers"

   The `r.jina.ai` render of the same page reported it could find **no** sentence stating a
   distinct-paper count, and returned **ABSENT** when asked whether **53** appears anywhere as a
   count of papers. `src-0038` states **53**, two-form confirmed on its own two artefacts.

   **THE 51 IS ONE-FORM AND IS NOT A CLAIM OF THIS BASE. IT MUST NOT BE CITED.** What *is*
   established is narrower: `src-0038.md`'s stored ruling that 51 is a search-tool artefact and
   *"is not a claim of this base"* **is no longer safe**, because 51 now has a candidate
   provenance in the vendor's own report — the very document `src-0038` is downstream of.

5. **NEGATIVE RESULT — WHY THIS SOURCE DOES NOT HELP
   `institutional-incident-real-world-impact`.** Asked unprompted which of EY, KPMG, PwC, Deloitte
   and ENISA the page names: **EY ABSENT, PwC ABSENT, ENISA ABSENT**. KPMG appeared **only as a
   related-article link title** — *"Chasing the Hallucinations: KPMG's AI-Powered Attempt at
   'Redefining Excellence'"* — and Deloitte **only as a related-article link**, *"The Deloitte
   Citation Situation - $98K Controversy Explained"*,
   `https://gptzero.me/news/deloitte-australia-citation-check/`. **No finding about any of this
   base's four responding institutions appears on this page.**

# Quotes with locations

Headline (direct render):
> "GPTZero finds 100 new hallucinations in NeurIPS 2025 accepted papers"

Bibliographic origin (`r.jina.ai` render of `https://arxiv.org/pdf/2602.05930`, reference list):
> "[1] GPTZero. GPTZero finds 100 new hallucinations in NeurIPS 2025 accepted papers.
> https://gptzero.me/ news/neurips/ , 2026. Accessed: 2026-02-05."

Denominator, verification step and the one-form 51 sentence: quoted in full under key claims 2, 3
and 4 above.

# Limitations

- **ACADEMIC CORPUS ONLY.** NeurIPS conference papers. `src-0038`'s scope caveat — *nothing here
  transfers to the corporate-report population* — applies to this source identically, and it is
  the reason key claim 5 is a negative result.
- **THE 51 IS ONE-FORM AND BANNED FROM CITATION** until `ctr-0061` step (i) is run. The two render
  families **disagree about whether the page states a distinct-paper count at all**, which is a
  stronger caution than a mere numeric discrepancy. Per carry-forward `[205]` neither render
  family outranks the other and disagreements are recorded as disagreements.
- **COUNTS ARE THE KNOWN-WEAK CASE FOR THIS TOOLING.** `ctr-0051` established that **presence is
  recoverable from this tooling but counts are not**. Key claim 4 is exactly a count, and the
  honest expectation for `ctr-0061` step (i) is a *confirmed persistent disagreement* rather than
  a resolution.
- **A VENDOR SELF-REPORT.** This is GPTZero describing GPTZero's own work. It is the **ancestor**
  of `src-0012`, `src-0034`, `src-0035`, `src-0036`, `src-0037`, `src-0038` and `src-0040`, not an
  independent check on any of them. **Acquiring it cannot relieve any independence bar, by
  construction** — that is the point key claim 5 records, and `incident-evidence-vendor-concentration`
  governs.
- **THE TABLE WAS NOT ENUMERATED.** The page carries a table of the hallucinations. Cycle 114 did
  **not** ask either render to enumerate it. That enumeration is the better instrument for the
  count question and is named as `ctr-0061` step (i).
- **NO PER-INSTITUTION FINDING, NO REMEDY DATA, NO RESPONSE STATEMENT.** Nothing here bears on how
  any institution responded when caught.
