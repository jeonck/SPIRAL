---
url: https://arxiv.org/abs/2605.06910
title: "Benchmarking Large Language Models for IoC Recovery under Adversarial Code Obfuscation and Encryption"
type: benchmark
accessed: 2026-07-29
added_cycle: 9
---

## Summary

arXiv 2605.06910v1 (submitted 7 May 2026, cs.CR, CC BY 4.0, 11 pages / 2 figures /
8 tables). Authors: Jaime Morales, Sergio Pastrana, Juan Tapiador (Universidad Carlos
III de Madrid, Spain) — a third team, independent of both src-0003's and src-0007's
authors.

Added opportunistically in cycle 9's T3 alongside src-0007. Scope note, read this
before citing it: this paper measures IoC recovery from **JavaScript source code under
deliberate adversarial concealment**, NOT IoC extraction from narrative threat reports.
It is therefore NOT a replication of src-0003 and must not be used as one. Its value to
this topic is as a boundary condition on the "mechanical extraction is the reliable
part of CTI" hypothesis, and as the knowledge base's first evidence on adversary-side
evasion of LLM extraction — a threat model no other collected source covers.

## Key claims

1. Recognising an IoC that is present in plain text is essentially free for current
   LLMs: at baseline (P0) and through four levels of syntactic obfuscation (P1-P4,
   e.g. variable renaming, Base64 encoding), detection is ~100% for ChatGPT (GPT-4
   Turbo), Claude 3 Sonnet, Gemini 1.5 Pro and Grok-2.
2. Cryptographic concealment collapses this to near zero: at P5-P6 (XOR and AES-256)
   detection falls to roughly 0-1%, and the failure persists through the combined
   transformations P7-P12 (~95%+ misses). The authors frame encryption as the critical
   gap in LLM-based code analysis.
3. Hallucination rates (reporting an IoC that is not there) were low but nonzero and
   varied by vendor: Anthropic 0.11%, ChatGPT 0.23%, Gemini 4.8%, Grok and Cohere 0%.
4. The paper does not cite or compare against LANCE or PRISM (arXiv 2506.11325).

## Relevant quotes

Abstract (verbatim):

> "Software obfuscation and encryption present persistent challenges for program
> comprehension and security analysis, particularly when adversaries conceal Indicators
> of Compromise (IoCs) such as IP addresses within source code. While Large Language
> Models (LLMs) have recently demonstrated remarkable progress in code reasoning and
> transformation, their resilience against adversarial concealment techniques remains
> largely uncharted. This paper introduces a systematic benchmark for secret detection
> under adversarial code transformations, designed to evaluate the capacity of LLMs to
> recover IoCs embedded in obfuscated and encrypted JavaScript programs. We construct a
> dataset of 336 programs, progressively transformed through 12 levels of obfuscation
> and cryptographic concealment (including XOR and AES-256), to emulate realistic threat
> scenarios. An automated evaluation framework standardizes LLM queries and responses,
> enabling reproducible, large-scale testing across diverse models. Our results reveal a
> dichotomy: while LLMs exhibit high success against lightweight transformations such as
> variable renaming and Base64 encoding, encryption-based concealment severely degrades
> detection performance."

## Limitations of this source

- **Different task from the rest of this issue.** Input is source code, not a threat
  report. The metric is detection rate for a known planted indicator, not precision or
  recall over an open set of candidate indicators in prose. The near-100% plain-text
  figure therefore does NOT corroborate src-0003's 97.6% F1: finding one planted IP in
  a program is not the same problem as separating malicious indicators from benign
  artifacts in a report, which is the specific difficulty src-0003 exists to address.
- Single indicator family in the concealment experiments (IP addresses); synthetic
  transformation pipeline rather than captured real-world malware.
- 336 programs is small, and the paper's own framing is a first systematic look at an
  "largely uncharted" question.
- Retrieved via automated fetch of the arXiv /html and /abs pages; the abstract was
  captured verbatim, but the per-level numbers above came from the fetcher's reading of
  the results tables and were not re-checked row-by-row against the PDF. Treat the
  ~100% / ~0-1% / hallucination-rate figures as approximate pending a verbatim table
  pull, and re-verify before they are used to support a scored resolution.

## Cycle-29 re-verification (T4 G2) — APPEND ONLY, nothing above is altered

Re-checked on 2026-07-30 against `https://arxiv.org/html/2605.06910v1`,
`https://arxiv.org/html/2605.06910` and `https://arxiv.org/abs/2605.06910` — three
fetches, each instructed to quote verbatim and to write `ABSENT` or `CANNOT READ` rather
than infer. This was the stalest source in the base (last verified cycle 10) and its own
limitations section asked for exactly this pull before its numbers supported a scored
resolution.

**Version staleness:** `/abs` lists **v1 only**, submitted 7 May 2026, "11 pages, 2
figures, 8 tables". No revision since collection.

### 1. FAILED — key claim 1 is over-general and omits the fifth evaluated model

The paper evaluates **five** models, not four. Body text, verbatim:

> "ChatGPT, Claude, Gemini and Grok achieve 100% across all four phases without reporting
> uncertainties"

> "Cohere, however, shows progressive degradation: 1% missed detections in P1, 2% in P2,
> 5% in P3, and in P4, 65% misses plus 35% explicit 'Don't Know' responses"

P1–P4 are **purely syntactic and contain no encryption** (Table 3, verbatim: "1. Base64
encoding", "2. Identifier obfuscation", "3. Dead code injection", "4. Structural
obfuscation"). So the stored generalisation — that recognising a plain-text IoC "is
essentially free for current LLMs" — is **contradicted by the paper's own legible body
text** for one of its five models, at a phase with no cryptography in it. The clean
"encryption is the boundary" reading that `ioc-extraction-reliability`'s third
candidate_resolution leans on is **not what the paper shows**. `ctr-0007` opened.

### 2. RESOLVED — the phase-label discrepancy is a self-contradiction IN THE SOURCE, and our mapping is the correct one

Carry-forward **[5]**, open since cycle 10 and believed to need PDF access, is
**discharged from HTML alone**. Table 3 assigns "5. XOR encryption", "6. AES-256
encryption", "7. XOR + simple obfuscation", "8. AES + simple obfuscation". Body text
agrees twice:

> "In P5 (XOR encryption with the key embedded in the code), all LLMs transition from
> systematic success to near-complete failure"

> "A similar pattern emerges in P6 (AES-based encryption)"

A third body sentence contradicts both:

> "Both XOR (P5, P6) and AES-256 (P7, P8) produce nearly identical failure rates"

Two statements against one: **key claim 2's XOR = P5 / AES-256 = P6 mapping stands.**
Probable cause, offered as inference and not as fact: Table 3's own row descriptions
cross-reference level numbers shifted by **+1** throughout — row 7 says "the XOR-encrypted
IoC (level 6)", row 8 "AES encryption (level 7)", row 9 "injected dead code (level 4)",
row 11 "the advanced structural obfuscations of level 5" — consistent with a numbering in
which the baseline is level 1. **No contradiction entry on this limb**: the conflict is
internal to the source and our state records the majority reading (carry-forward [32]'s
filing test).

### 3. NEW DEFECT IN THE SOURCE — it defines its metrics twice, incompatibly

Body text, verbatim: *"Detection rate, i.e., the proportion of samples in which the model
correctly identifies the presence of an IoC"* and *"Extraction accuracy, i.e., the
proportion of samples in which the recovered IP matches the ground truth exactly."*
Table 6's caption, verbatim: *"DR reflects the detection rate, i.e., ratio of YES an
answers. The Acc. reflects the percentage of correct detections among those positive
answers."* A ratio of YES answers **does not require correctness**, and `Acc.`'s
denominator moves from all samples to positive answers only. **Whether this paper's "100%
detection" means 100% correctly identified or 100% answered YES is undetermined by the
paper.**

### 4. NEW READABLE CONTENT — Table 6, never captured in 20 cycles

> "Table 6. Overall results per model. DR reflects the detection rate, i.e., ratio of YES
> an answers. The Acc. reflects the percentage of correct detections among those positive
> answers. Finally, the #DK reflects the number of "Don't Know" responses."

| Model | #Queries | DR | Acc. | #DK |
|---|---|---|---|---|
| Anthropic | 4,358 | 38.5% | 99.7% | 94 |
| ChatGPT | 4,362 | 38.6% | 99.4% | 37 |
| Gemini | 4,358 | 38.5% | 87.7% | 0 |
| Grok | 4,357 | 35% | 100.0% | 0 |
| Cohere | 4,323 | 22.8% | 100.0% | 625 |

**Aggregates over all thirteen phases — never quote them as per-phase results.**
Gemini's 87.7% `Acc.` is the same model that carries the 4.8% hallucination rate; Cohere's
625 "Don't Know" responses are consistent with the P4 degradation above.

### 5. WHAT IS STILL IMAGE-LOCKED, and what that costs

The **per-phase P0–P12 breakdown exists only inside Figure 2** ("LLM detection outcomes
across all obfuscation phases (P0–P12)"), confirmed by two fetches instructed to write
`CANNOT READ`. So the stored **"roughly 0–1%"** at P5–P6 and **"~95%+ misses"** at P7–P12
are **figure-derived and not text-verified** and must be cited as such. What the body text
supports verbatim is qualitative only: "near-complete failure", "encrypted IoCs become
effectively opaque", "once the code is encrypted, detection of IoCs remains essentially
zero for all models". This is the **fourth** figure-locked-numbers instance in this base
after src-0003, src-0005 and src-0018.

**PASSED:** key claim 3's hallucination rates are now **text-confirmed exactly** — Table 7,
verbatim "Anthropic | 0.11 | ChatGPT | 0.23 | Gemini | 4.8 | Grok | 0 | Cohere | 0", caption
"While aggregate rates remain below 5%, their non-trivial presence in cybersecurity
workflows is significant." The abstract re-verified verbatim, word for word, against the
version stored above. Authors and dataset size (336) confirmed. The paper's only stated
limitation: *"A practical limitation of our study is that the evaluated models correspond
to commercial releases available at the time of experimentation, and some of them have
since been superseded by newer versions."*

---

## CYCLE-52 APPEND — ctr-0019 STEP (i). NOTHING ABOVE IS REMOVED OR REWRITTEN.

Knowledge is append-only, so **key claim 1 above is left exactly as written**. Read it together
with this section, which records the sentence it omits.

### KEY CLAIM 5 (new, appended cycle 52 under ctr-0019 step (i))

**At the unobfuscated baseline P0, the paper asserts 100% detection for ALL FIVE evaluated
models, Cohere included.** Verbatim, and this is the FIRST sentence of the Results paragraph
from which cycles 29 and 35 took their quotations:

> "all evaluated LLMs achieve 100% detection when the IoC appears in plain text"

The whole paragraph, in order, reproduced as printed:

> "The baseline scenario (P0) serves as a baseline: all evaluated LLMs achieve 100% detection
> when the IoC appears in plain text, confirming their capability to identify and extract IoCs in
> source code. As obfuscation techniques progress through simple transformations--P1 (Base64
> encoding), P2 (identifier obfuscation), P3 (dead code injection), and P4 (advanced structural
> obfuscation)--all most models perform well. Indeed, ChatGPT, Claude, Gemini and Grok achieve
> 100% across all four phases without reporting uncertainties. Cohere, however, shows progressive
> degradation: 1% missed detections in P1, 2% in P2, 5% in P3, and in P4, 65% misses plus 35%
> explicit Don't Know responses."

("all most models" is reproduced as printed. It appears to be a typographical error in the source
for "almost all models"; this entry does not silently repair it.)

### WHAT THIS CHANGES ABOUT KEY CLAIM 1, STATED PRECISELY

Key claim 1 spans "at baseline (P0) and through four levels of syntactic obfuscation (P1-P4)"
while naming only four models. **That span is correct at P1-P4 and incorrect at P0.**

- **At P0:** all five models, Cohere included, are stated at 100%. Key claim 1's omission of
  Cohere is wrong here.
- **At P1-P4:** the four-versus-one split holds. Cohere degrades 1% / 2% / 5% / (65% misses +
  35% Don't Know).

The paper states **no P0-specific per-model figure for Cohere** other than that collective
sentence (asked directly at cycle 51 and answered ABSENT). So Cohere's P0 value is available only
as a member of "all evaluated LLMs".

### CONSEQUENCE FOR ctr-0007, ctr-0019 STEP (iii)

ctr-0007 (opened 29, resolved 35) concluded that "the stored claim's generalisation to **current
LLMs** is contradicted by the paper it cites." **That verdict located the defect in the wrong
words.** On the narrowest reading of key claim 1's headline sentence — "Recognising an IoC that is
present in plain text is essentially free for current LLMs" — the paper asserts exactly that
generalisation, in its own words, for all five models, at P0. What is actually defective in key
claim 1 is the **CONFLATION OF P0 WITH P1-P4** under one four-model span, not the phrase "current
LLMs".

**This does NOT reinstate key claim 1.** It relocates the error. Anywhere ctr-0007 is relied on,
this relocation must travel with it.

### IT ALSO STRENGTHENS CYCLE 35's ANALYSIS RATHER THAN WEAKENING IT

Cycle 35 ruled that model-side variance under purely syntactic noise undercuts the scaffolding
hypothesis, on the ground that "five models, one corpus, one task, NO scaffolding on any arm, and
a spread running from 100% to roughly 35% at P4" could not exist if the model were not what sets
reliability. The omitted sentence supplies the **baseline control that argument was missing**:
because all five models sit at 100% at P0, the entire P1-P4 divergence is attributable to the
transformation interacting with model identity, and **not** to any difference in baseline
extraction capability between the models. That is a stronger form of cycle 35's argument than
cycle 35 was able to make.

### THE TWO STANDING CITATION CONSTRAINTS ARE UNAFFECTED

The P0 sentence is TEXT-verified, unlike the per-phase P5-P12 percentages, which remain
FIGURE-DERIVED and image-locked in Figure 2. And the metric ambiguity still travels with it: the
paper defines detection rate twice, incompatibly (body "the proportion of samples in which the
model correctly identifies the presence of an IoC" vs Table 6's "ratio of YES an answers"), so
whether "100% detection" at P0 means correctly-identified or answered-YES is **undetermined by
the paper**. Both incompatible definitions were re-confirmed PRESENT at cycle 51.

**Scope, unchanged:** src-0008's task is recovering a planted indicator from JavaScript SOURCE
CODE. It remains ILLUSTRATION ONLY for any candidate about extraction from NARRATIVE THREAT
REPORTS, per cycle 35's scope ruling.

**Provenance of these strings:** verified by cycle 51 at two URL forms
(https://arxiv.org/html/2605.06910v1 and https://arxiv.org/html/2605.06910), PRESENT verbatim on
an exact-string pass. Cycle 52 did not re-fetch; it records what cycle 51 verified.
