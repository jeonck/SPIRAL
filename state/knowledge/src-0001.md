---
url: https://arxiv.org/abs/2503.23175
title: "Large Language Models Are Unreliable for Cyber Threat Intelligence"
type: paper
accessed: 2026-07-26
added_cycle: 1
---

## Summary

Authors Emanuele Mezzi, Fabio Massacci, and Katja Tuma (arXiv preprint, cs.CR,
2503.23175) present an evaluation methodology for testing state-of-the-art LLMs on
real-size (not toy/snippet) CTI report tasks. They test three LLMs across zero-shot,
few-shot, and fine-tuning paradigms, and explicitly measure two properties beyond raw
accuracy: **consistency** (does the model give the same answer to the same/paraphrased
input across repeated runs) and **confidence calibration** (does the model's stated
confidence track its actual correctness). The central claim of the paper is that this
combination of properties — not just point accuracy — is what makes current LLMs
unsuitable for production CTI use, because inconsistent/overconfident systems degrade
analyst trust and cannot be safely automated without a human in the loop.

## Key claims

1. "LLMs cannot guarantee sufficient performance on real-size reports while also being
   inconsistent and overconfident" — i.e., the failure mode is not purely accuracy but
   a combination of unreliability across repeated queries and miscalibrated confidence,
   evaluated on 350 real-world threat intelligence reports.
2. Few-shot learning and fine-tuning "only partially improve the results," suggesting
   the unreliability is not simply a data/prompting problem that scales away with more
   examples — it persists across all three tested learning paradigms.
3. The paper frames reliability for CTI as a multi-dimensional property (accuracy +
   consistency + calibration), which is a stronger bar than benchmarks that report only
   task accuracy.

## Relevant quotes

- "LLMs cannot guarantee sufficient performance on real-size reports while also being
  inconsistent and overconfident." (abstract)
- "Few-shot learning and fine-tuning only partially improve the results." (abstract)

## Limitations of this source

- Retrieved via automated abstract/page summarization tooling, not a full manual read
  of the PDF; exact numeric results per model/task (e.g., specific consistency or
  calibration scores) were not captured and should be pulled from the full paper in a
  future cycle if fine-grained numbers are needed.
- Preprint (arXiv), not yet confirmed as peer-reviewed/published at a venue as of
  access date.
- Only three (unnamed, in the fetched summary) LLMs were tested — model identities and
  count should be verified against the full text before drawing generalizations to
  "LLMs" as a class.

## Additional findings (cycle 3)

Full-text retrieval succeeded this cycle via the arXiv HTML render
(https://arxiv.org/html/2503.23175v1), resolving several open questions left from cycle
1's abstract-only summary. Note: extracted via automated HTML-to-summary tooling (a
fast model reading the rendered HTML), not a manual cell-by-cell read of the PDF
tables — treat exact numbers as high-confidence but not independently hand-verified,
the same caveat already applied to src-0002's table data.

**(1) The 3 LLMs tested**: gpt4o (OpenAI), gemini-1.5-pro-latest (Google), and
mistral-large-2 (Mistral). Notably, none of these three overlap with the model lists
used in src-0002 (GPT-4-turbo, GPT-3.5-turbo, Gemini-1.5, LLAMA3-70B, LLAMA3-8B) or
src-0005 — direct cross-paper comparison at the per-model level is not possible with
current evidence.

**(2) Consistency methodology and numbers**: measured via bootstrapped confidence
intervals (CI) across 10 re-prompts per input, rather than a simple "% agreement"
metric.
- Information Extraction: max CI width 0.02 (e.g. Gemini campaign-retrieval
  precision/recall CI [0.73, 0.74]) — comparatively tight/consistent.
- Information Generation: max CI width 0.06 (e.g. Gemini CVE-generation recall CI
  [0.19, 0.25], described in the paper as a "27.27% percentage difference") —
  noticeably less consistent than extraction.

**(3) Calibration numbers (Table 6, ECE / Brier Score, reported for gpt4o)**,
zero-shot → fine-tuned:
- Information Extraction — Campaign: ECE 0.25 → 0.48; BS 0.26 → 0.48.
- Information Extraction — APT: ECE 0.16 → 0.25; BS 0.15 → 0.23.
- Information Extraction — CVE: ECE 0.28 → 0.18; BS 0.32 → 0.21 (the one case where
  fine-tuning improved calibration).
- Information Generation — CVE: ECE 0.15 → 0.91; BS 0.29 → 0.98 (near-total
  miscalibration after fine-tuning).
- Information Generation — Attack vector: ECE 0.47 → 0.87; BS 0.43 → 1.00.
(Higher ECE/BS = worse calibration; 0 = perfect calibration.)

**(4) Zero-shot vs. few-shot vs. fine-tuned, specifically on consistency/calibration
and accuracy** (Table 4 / Table 7 samples, precision/recall, zero-shot → few-shot →
fine-tune):
- Campaign (gpt4o): 0.72/0.72 → 0.72/0.72 → 0.58/0.58.
- Campaign (gemini): 0.77/0.77 → 0.73/0.73 → 0.61/0.61.
- APT (gpt4o): 0.87/0.87 → 0.84/0.84 → 0.68/0.68.
- CVE extraction (mistral): 0.72/0.90 → 0.79/0.91 → 0.71/0.69.
- Info-generation "Labels" (gpt4o): 0.50/0.50 → 0.44/0.44 → 0.44/0.44.
- Info-generation CVE (gpt4o): 0.10/0.06 → 0.08/0.07 → 0.00/0.00 (complete failure
  after fine-tuning).
- The paper additionally reports few-shot reduced APT-extraction performance by 7.87%
  (0.89 → 0.82, gemini/mistral) and fine-tuning caused a 21.84% precision/recall drop
  for APT extraction (gpt4o/mistral); few-shot also worsened CVE-extraction ECE
  (0.28 → 0.35).

**Key sharpening of the original claim**: the abstract says few-shot/fine-tuning "only
partially improve" results. The full-text numbers show this is generous — on several
sub-tasks (APT extraction, CVE generation, attack-vector generation), fine-tuning and
even few-shot prompting made accuracy and/or calibration measurably *worse*, not just
insufficiently better. This is a meaningful refinement, not a contradiction, since the
abstract's claim ("only partially improve") is still directionally consistent with
"sometimes no improvement or regression."

## Cycle-25 G2 re-verification (APPENDED, nothing above removed or rewritten)

Re-fetched `https://arxiv.org/html/2503.23175` (whole-table pull + exact-string check +
metric definitions in one call) and `https://arxiv.org/abs/2503.23175` (abstract). Last
prior verification was cycle 7, making this the stalest source in the base at the time.

### 1. Provenance was WRONG and is corrected here

The "Limitations of this source" section above says "Preprint (arXiv), not yet confirmed
as peer-reviewed/published at a venue as of access date." That was accurate at cycle 1 and
is **wrong now**. The page carries, verbatim:

> Citation: Mezzi, E., Massacci, F., & Tuma, K. (2025, August). Large language models are
> unreliable for cyber threat intelligence. In International Conference on Availability,
> Reliability and Security (pp. 343-364). Cham: Springer Nature Switzerland. Conference
> version DOI: https://doi.org/10.1007/978-3-032-00627-1_17.

**This source is peer-reviewed and published (ARES 2025).** Any cycle comparing provenance
strength across this base must use that, not "preprint".

### 2. TABLE 6 IN FULL — nine rows, not the five stored above

Caption, verbatim: "ECE and BS with gpt4o for information generation and extraction."
**gpt4o ONLY.** There is no ECE or Brier figure for gemini-1.5-pro-latest or
mistral-large-2 anywhere in the paper.

| | zero-shot ECE/BS | few-shot ECE/BS | fine-tuning ECE/BS |
|---|---|---|---|
| **Extraction** campaign | 0.25 / 0.26 | 0.26 / 0.28 | 0.48 / 0.48 |
| **Extraction** APT | 0.16 / 0.15 | 0.17 / 0.15 | 0.25 / 0.23 |
| **Extraction** CVE | 0.28 / 0.32 | 0.35 / 0.37 | 0.18 / 0.21 |
| **Extraction** attack vector | 0.13 / 0.46 | 0.19 / 0.49 | 0.27 / 0.58 |
| **Generation** goals | 0.13 / 0.14 | 0.04 / 0.03 | 0.08 / 0.05 |
| **Generation** labels | 0.45 / 0.44 | 0.57 / 0.53 | 0.48 / 0.49 |
| **Generation** country | 0.19 / 0.22 | 0.38 / 0.27 | 0.35 / 0.29 |
| **Generation** CVE | 0.15 / 0.29 | 0.13 / 0.22 | 0.91 / 0.98 |
| **Generation** attack vector | 0.47 / 0.43 | 0.48 / 0.42 | 0.87 / 1.00 |

**Every cell previously stored in section (3) above is EXACT.** What was wrong was not the
values but the *coverage*: four rows and the whole few-shot column had never been captured,
and the missing rows are the ones that qualify the claim — see below.

### 3. The extraction-vs-generation calibration gloss does not survive the full table

Section (3) above, and `index.json` key_claims[4], say calibration was "markedly worse for
information-generation than information-extraction". **At zero-shot this does not hold
row-wise:** generation goals 0.13, CVE 0.15 and country 0.19 are all better calibrated than
extraction campaign 0.25 and extraction CVE 0.28 — and the claim's own worked example, CVE
generation at 0.15, beats CVE extraction at 0.28.

What the table does support: the generation **mean** ECE exceeds the extraction mean at
every paradigm, small at zero-shot (0.278 vs 0.205) and large after fine-tuning (0.538 vs
0.295), with the extreme miscalibration in exactly two fine-tuned generation rows.
**These four means are DERIVED from the printed cells by the cycle-25 agent and are NOT
printed in the paper.** Contradiction **`ctr-0003`** is opened for this. Nothing above has
been rewritten.

The stronger surviving finding is about **direction of change, not task family**: relative
to zero-shot, fine-tuning worsened ECE on **7 of the 9 rows** and BS on the same 7 (derived
by cell comparison), improving only extraction CVE (ECE 0.28->0.18, BS 0.32->0.21) and
generation goals (ECE 0.13->0.08, BS 0.14->0.05).

### 4. The consistency protocol is STRONGER than this file recorded

Verbatim, and previously held here only as a paraphrase:

> For each task, we re-prompt each LLM ten times on the same input, with temperature=0 and
> the same seed to guarantee maximum determinism, derive precision, recall, and f1-score,
> and then employ the bootstrapping method to empirically build CIs.

**temperature=0 AND a fixed seed.** The CI widths of 0.02 (extraction) and 0.06
(generation) are therefore residual non-determinism *under maximal determinism settings*,
which strengthens the consistency finding rather than weakening it.

Metric definition, verbatim: "The ECE and BS, are two measures of calibration that quantify
the deviation from perfect calibration." (No formula was returned; the paper's definition
as fetched is this sentence.)

### 5. Exact-string results

PRESENT and verbatim-confirmed: `0.91`, `0.98`, `0.47`, `0.87`, `0.68`, `350` ("dataset of
350 threat intelligence reports"), `27.27` ("27.27% percentage difference between the lower
and upper bound"), `7.87` ("performance decreases by 7.87% from 0.89 to 0.82"), `21.84`,
and "Few-shot learning and fine-tuning only partially improve the results".

**FALSE-NEGATIVE WARNING, important beyond this source.** The same `/html` fetch reported
"inconsistent and overconfident" as **ABSENT**. The `/abs` fetch returned the abstract in
full: "We show how LLMs cannot guarantee sufficient performance on real-size reports while
also being inconsistent and overconfident." **The stored quotation is exact and the ABSENT
verdict was wrong.** A single fetch's ABSENT is not evidence of absence — confirm against a
second URL form before recording any quotation defect.

### 6. Fine-tuning dataset size: still not captured

The paper **nowhere** attributes the post-fine-tuning degradation to dataset size or
overfitting (checked explicitly). The only sentence bearing on the split came back
**elided** — "We randomly select 70% of the dataset...70% to the few-shot examples section
and fine-tuning" — so it is **not verbatim** and must be re-pulled before use. The
fine-tuning pool is bounded above by the 350-report dataset either way.
