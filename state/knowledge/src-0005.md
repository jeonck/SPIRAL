---
url: https://arxiv.org/abs/2509.20166
title: "CyberSOCEval: Benchmarking LLMs Capabilities for Malware Analysis and Threat Intelligence Reasoning"
type: benchmark
accessed: 2026-07-26
added_cycle: 1
---

## Summary

CyberSOCEval (arXiv 2509.20166) is an industry-originated benchmark (large author list
including senior authors from CrowdStrike, e.g. Sven Krasser, and Meta, e.g. Joshua
Saxe), released as part of Meta's open-source "CyberSecEval 4" suite. It targets two
SOC-defender-relevant domains the authors say are under-covered by existing
benchmarks: malware analysis and threat-intelligence reasoning. Unlike src-0002
(CTIBench, mid-2024 models), this benchmark evaluates newer/larger LLMs including
reasoning models with test-time scaling, giving a more current (2025-era) data point on
whether model scaling is closing the CTI reliability gap.

## Key claims

1. Larger/more modern LLMs perform better on CyberSOCEval's malware-analysis and
   threat-intelligence-reasoning tasks, "confirming the training scaling laws
   paradigm" — i.e. raw scale continues to help on these tasks as of the 2025 model
   generation tested.
2. Reasoning models using test-time scaling do NOT get the same performance boost on
   these CTI/malware tasks that they get on coding and math benchmarks — suggesting
   generic "reasoning" post-training does not transfer well to cybersecurity threat
   analysis, and that domain-specific training/data is likely still necessary.
3. "Current LLMs are far from saturating our evaluations," i.e. as of this benchmark's
   publication, no evaluated model is near a ceiling — there remains substantial
   measured headroom/unreliability in both malware analysis and threat-intel reasoning
   tasks industry-wide, not just in academic benchmarks like CTIBench.

## Relevant quotes

- "core defensive domains with inadequate coverage in current benchmarks" (stated
  motivation for building CyberSOCEval).
- "larger, more modern LLMs tend to perform better, confirming the training scaling
  laws paradigm."
- "reasoning models leveraging test time scaling do not achieve the same boost as in
  coding and math."
- "Current LLMs are far from saturating our evaluations."

## Limitations of this source

- Retrieved via automated abstract-page summarization; exact model names and
  per-model/per-task numeric scores were not captured in this pass and should be
  pulled from the full paper before citing specific figures.
- Industry-affiliated authorship (CrowdStrike, Meta) — the benchmark's task design and
  framing may reflect these organizations' commercial priorities (e.g. SOC tooling
  relevant to CrowdStrike's product line); treat comparative claims about specific
  vendors' models with that in mind.
- As with CTIBench (src-0002), this is a benchmark snapshot tied to the specific model
  set evaluated at publication time (2025); results will age as new model generations
  release.

---

## Appendix — cycle 26 G2 re-verification (APPENDED, nothing above altered)

Re-verified 2026-07-30 by the cycle-26 T4 under the G2 rule. Two independent fetches:
`arxiv.org/abs/2509.20166` (abstract + metadata) and `arxiv.org/html/2509.20166v1`,
plus a third confirming fetch of `arxiv.org/html/2509.20166v2`. This source was last
verified at cycle 9 and was by a wide margin the stalest in the base.

### Verdict: PASS. All three stored key claims and all four stored quotations verified.

Exact-string check against the abstract, all four PRESENT verbatim:

- "core defensive domains with inadequate coverage in current benchmarks" — PRESENT
- "larger, more modern LLMs tend to perform better, confirming the training scaling
  laws paradigm" — PRESENT
- "reasoning models leveraging test time scaling do not achieve the same boost as in
  coding and math" — PRESENT
- "Current LLMs are far from saturating our evaluations" — PRESENT

One wording note, not a defect: the stored quotations are drawn from the ABSTRACT. The
body paraphrases two of them differently — the introduction reads "do not achieve the
boost they do in areas like coding and math" and "core defensive domains that have
inadequate coverage in current security benchmarks". Anyone re-checking these strings
against the body rather than the abstract will get a false ABSENT.

### Carry-forward [10] is DISCHARGED, and the answer is that it was never achievable

[10] had asked since cycle 7 for src-0005's per-model numeric scores. **The paper
publishes no results table.** Every per-model number is inside a figure image:
Fig. 8 (MA accuracy by difficulty), Fig. 9 (TIR by report source), Fig. 12 (MA accuracy
+ Jaccard), Fig. 13 (MA by malware family), Fig. 14 (MA by question topic), Fig. 15 and
Fig. 16 (per-model accuracy + Jaccard). Both HTML fetches, instructed to answer
"CANNOT READ" rather than infer, did so; the v2 fetch was run specifically to satisfy
the cycle-25 rule that a single fetch's negative verdict is unreliable. The only tables
are Table 1 (malware category descriptions) and appendix code listings. No sentence
anywhere pairs a model name with a score, and no numeric Jaccard value appears in text.

This is a permanent limit for this agent by this route, of the same kind already
recorded for src-0003's Figure 6 and src-0018's four artefacts. It should not be
re-attempted without a new route (published raw results, the CyberSecEval 4 repo, or a
reader that can do OCR).

### First numbers ever captured from this source, in 26 cycles

Aggregate ranges ARE in the text, and were missed by every prior pass:

- **Malware Analysis** — Section 4.1, verbatim: "The overall accuracy scores range from
  approximately 23-34%, which should be compared to a baseline of approximately 0.63%
  accuracy that would be expected from purely random guessing (see Appendix B)."
- **Threat Intelligence Reasoning** — Section 4.2, verbatim: "Overall scores range from
  approximately 43%-53%, which should be compared to a baseline of approximately 1.7%
  accuracy that would be expected from purely random guessing (see Appendix C)."

These are ranges across the model set, not per-model values, and the paper names neither
the best nor the worst model in text. The random baselines are tiny because the format is
multi-select and scoring is all-or-nothing.

### Task format, construction and metric — never previously recorded, and material

- Both benchmarks are **multi-select multiple choice**, not free-form extraction or
  generation. Metric verbatim: "Evaluation is based on accuracy: the share of questions
  for which the system selects all correct options and only the correct options."
- 609 malware-analysis Q&A test cases from detonated public Windows malware samples.
- 588 threat-intelligence-reasoning Q&A pairs from 45 distinct threat-intelligence
  reports; the report is supplied to the model "via a set of images (one per report
  page)".
- Questions were **LLM-generated then human-validated**: Llama 3.2 90B (malware
  analysis); "We used Llama 4 Maverick and Llama 3.2 90B with two strategies for
  synthetically generating questions" — Category-Based and Relationship-Based —
  "which we then sampled from and manually validated" (threat intelligence).

Consequence for cross-source comparison: src-0005's percentages are **not commensurable**
with src-0002's CTI-ATE F1 or src-0007's precision/recall, which score free-form output.
This is the same task-format non-commensurability cycle 22 established for src-0003's
97.6% classification score, and it is now the third instance in this base.

### The paper's own stated limitations, verbatim — none of them were in this base

- "We acknowledge that the use of multiple choice format in our evaluations does not
  provide a perfect proxy for capabilities"
- "It is important to also recognize the existence of performance bias that may be
  introduced in cases where the model under test is the same, or has similarities with
  the set of models that were used in synthetic data generation pipelines"
- "Another important limitation to call out is that the contextual data provided to an AI
  system under test in each of our benchmarks may differ significantly from the
  contextual data that a given business or security operations center observes in
  production."

The second is a live contamination concern, because the question generators are Llama
models and Llama models are among those under test.

### ATT&CK coverage — confirms an open question at source

This source reports **no ATT&CK-technique-mapping metric**. A targeted verbatim search
for "ATT&CK", "MITRE", "TTP", "technique" and "tactic" returned only the descriptive
phrase "understanding complex attack chains, and mapping tactics to frameworks like MITRE
ATT&CK" (one aspect among several that the TIR task is said to exercise) and an appendix
code listing retaining the keys "tactic, technique" from a `mitre_attcks` field.
`ttp-attack-mapping-reliability`'s open_question[0] already suspected this; it is now
settled at source.

### Provenance

v1 24 Sep 2025, v2 10 Nov 2025. No venue, no "accepted at" note, no DOI other than the
arXiv DOI; no affiliations are printed on the /abs page. So this is an **unreviewed
preprint** as far as this base can establish — the opposite direction from cycle 25's
finding that src-0001 is peer-reviewed at ARES 2025. The CrowdStrike/Meta affiliation
carried in this base since cycle 1 rests on identifying the senior authors (Sven Krasser,
Joshua Saxe), not on anything the paper prints. Per carry-forward [39], provenance
staleness is a base-wide problem and this is a second confirmed instance.

### No contradiction opened, and why

G3's trigger (prompts/system.md rule 3) is two SUPPORTED CLAIMS IN CONFLICT. Nothing
stored about this source is falsified — all three key claims and all four quotations hold
verbatim. What was found is missing context, not a conflict: the state never recorded the
multiple-choice format, the synthetic question generation, or the absence of an ATT&CK
metric, but it never asserted otherwise either, and `ttp-attack-mapping-reliability`'s
open_question[0] already flagged the last of the three. Following the precedent of
cycle 12 (ENISA's silence), cycle 21 (src-0016) and cycle 22 (src-0003's Figure 6), this
is recorded as a documented verification finding rather than a contradiction, so that the
G3 signal is not diluted.
